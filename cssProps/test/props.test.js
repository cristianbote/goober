/* Do not modify this file.  This file is generated from ./cssProps/templates/props.test.mustache */
import * as cssProps from '../src/props/index';
import { css } from 'goober';

describe('cssProps', () => {
    it('WebkitLineClamp type', () => {
        expect(typeof cssProps.WebkitLineClamp).toEqual('function');
    });

    it('WebkitLineClamp', () => {
        expect(cssProps.WebkitLineClamp('value1', 'value2')).toEqual({
            WebkitLineClamp: 'value1 value2'
        });
    });
    it('accentColor type', () => {
        expect(typeof cssProps.accentColor).toEqual('function');
    });

    it('accentColor', () => {
        expect(cssProps.accentColor('value1', 'value2')).toEqual({ accentColor: 'value1 value2' });
    });
    it('alignContent type', () => {
        expect(typeof cssProps.alignContent).toEqual('function');
    });

    it('alignContent', () => {
        expect(cssProps.alignContent('value1', 'value2')).toEqual({
            alignContent: 'value1 value2'
        });
    });
    it('alignItems type', () => {
        expect(typeof cssProps.alignItems).toEqual('function');
    });

    it('alignItems', () => {
        expect(cssProps.alignItems('value1', 'value2')).toEqual({ alignItems: 'value1 value2' });
    });
    it('alignSelf type', () => {
        expect(typeof cssProps.alignSelf).toEqual('function');
    });

    it('alignSelf', () => {
        expect(cssProps.alignSelf('value1', 'value2')).toEqual({ alignSelf: 'value1 value2' });
    });
    it('all type', () => {
        expect(typeof cssProps.all).toEqual('function');
    });

    it('all', () => {
        expect(cssProps.all('value1', 'value2')).toEqual({ all: 'value1 value2' });
    });
    it('animation type', () => {
        expect(typeof cssProps.animation).toEqual('function');
    });

    it('animation', () => {
        expect(cssProps.animation('value1', 'value2')).toEqual({ animation: 'value1 value2' });
    });
    it('animationDelay type', () => {
        expect(typeof cssProps.animationDelay).toEqual('function');
    });

    it('animationDelay', () => {
        expect(cssProps.animationDelay('value1', 'value2')).toEqual({
            animationDelay: 'value1 value2'
        });
    });
    it('animationDirection type', () => {
        expect(typeof cssProps.animationDirection).toEqual('function');
    });

    it('animationDirection', () => {
        expect(cssProps.animationDirection('value1', 'value2')).toEqual({
            animationDirection: 'value1 value2'
        });
    });
    it('animationDuration type', () => {
        expect(typeof cssProps.animationDuration).toEqual('function');
    });

    it('animationDuration', () => {
        expect(cssProps.animationDuration('value1', 'value2')).toEqual({
            animationDuration: 'value1 value2'
        });
    });
    it('animationFillMode type', () => {
        expect(typeof cssProps.animationFillMode).toEqual('function');
    });

    it('animationFillMode', () => {
        expect(cssProps.animationFillMode('value1', 'value2')).toEqual({
            animationFillMode: 'value1 value2'
        });
    });
    it('animationIterationCount type', () => {
        expect(typeof cssProps.animationIterationCount).toEqual('function');
    });

    it('animationIterationCount', () => {
        expect(cssProps.animationIterationCount('value1', 'value2')).toEqual({
            animationIterationCount: 'value1 value2'
        });
    });
    it('animationName type', () => {
        expect(typeof cssProps.animationName).toEqual('function');
    });

    it('animationName', () => {
        expect(cssProps.animationName('value1', 'value2')).toEqual({
            animationName: 'value1 value2'
        });
    });
    it('animationPlayState type', () => {
        expect(typeof cssProps.animationPlayState).toEqual('function');
    });

    it('animationPlayState', () => {
        expect(cssProps.animationPlayState('value1', 'value2')).toEqual({
            animationPlayState: 'value1 value2'
        });
    });
    it('animationTimingFunction type', () => {
        expect(typeof cssProps.animationTimingFunction).toEqual('function');
    });

    it('animationTimingFunction', () => {
        expect(cssProps.animationTimingFunction('value1', 'value2')).toEqual({
            animationTimingFunction: 'value1 value2'
        });
    });
    it('backdropFilter type', () => {
        expect(typeof cssProps.backdropFilter).toEqual('function');
    });

    it('backdropFilter', () => {
        expect(cssProps.backdropFilter('value1', 'value2')).toEqual({
            backdropFilter: 'value1 value2'
        });
    });
    it('backfaceVisibility type', () => {
        expect(typeof cssProps.backfaceVisibility).toEqual('function');
    });

    it('backfaceVisibility', () => {
        expect(cssProps.backfaceVisibility('value1', 'value2')).toEqual({
            backfaceVisibility: 'value1 value2'
        });
    });
    it('background type', () => {
        expect(typeof cssProps.background).toEqual('function');
    });

    it('background', () => {
        expect(cssProps.background('value1', 'value2')).toEqual({ background: 'value1 value2' });
    });
    it('backgroundAttachment type', () => {
        expect(typeof cssProps.backgroundAttachment).toEqual('function');
    });

    it('backgroundAttachment', () => {
        expect(cssProps.backgroundAttachment('value1', 'value2')).toEqual({
            backgroundAttachment: 'value1 value2'
        });
    });
    it('backgroundBlendMode type', () => {
        expect(typeof cssProps.backgroundBlendMode).toEqual('function');
    });

    it('backgroundBlendMode', () => {
        expect(cssProps.backgroundBlendMode('value1', 'value2')).toEqual({
            backgroundBlendMode: 'value1 value2'
        });
    });
    it('backgroundClip type', () => {
        expect(typeof cssProps.backgroundClip).toEqual('function');
    });

    it('backgroundClip', () => {
        expect(cssProps.backgroundClip('value1', 'value2')).toEqual({
            backgroundClip: 'value1 value2'
        });
    });
    it('backgroundColor type', () => {
        expect(typeof cssProps.backgroundColor).toEqual('function');
    });

    it('backgroundColor', () => {
        expect(cssProps.backgroundColor('value1', 'value2')).toEqual({
            backgroundColor: 'value1 value2'
        });
    });
    it('backgroundImage type', () => {
        expect(typeof cssProps.backgroundImage).toEqual('function');
    });

    it('backgroundImage', () => {
        expect(cssProps.backgroundImage('value1', 'value2')).toEqual({
            backgroundImage: 'value1 value2'
        });
    });
    it('backgroundOrigin type', () => {
        expect(typeof cssProps.backgroundOrigin).toEqual('function');
    });

    it('backgroundOrigin', () => {
        expect(cssProps.backgroundOrigin('value1', 'value2')).toEqual({
            backgroundOrigin: 'value1 value2'
        });
    });
    it('backgroundPosition type', () => {
        expect(typeof cssProps.backgroundPosition).toEqual('function');
    });

    it('backgroundPosition', () => {
        expect(cssProps.backgroundPosition('value1', 'value2')).toEqual({
            backgroundPosition: 'value1 value2'
        });
    });
    it('backgroundRepeat type', () => {
        expect(typeof cssProps.backgroundRepeat).toEqual('function');
    });

    it('backgroundRepeat', () => {
        expect(cssProps.backgroundRepeat('value1', 'value2')).toEqual({
            backgroundRepeat: 'value1 value2'
        });
    });
    it('backgroundSize type', () => {
        expect(typeof cssProps.backgroundSize).toEqual('function');
    });

    it('backgroundSize', () => {
        expect(cssProps.backgroundSize('value1', 'value2')).toEqual({
            backgroundSize: 'value1 value2'
        });
    });
    it('blockSize type', () => {
        expect(typeof cssProps.blockSize).toEqual('function');
    });

    it('blockSize', () => {
        expect(cssProps.blockSize('value1', 'value2')).toEqual({ blockSize: 'value1 value2' });
    });
    it('border type', () => {
        expect(typeof cssProps.border).toEqual('function');
    });

    it('border', () => {
        expect(cssProps.border('value1', 'value2')).toEqual({ border: 'value1 value2' });
    });
    it('borderBlock type', () => {
        expect(typeof cssProps.borderBlock).toEqual('function');
    });

    it('borderBlock', () => {
        expect(cssProps.borderBlock('value1', 'value2')).toEqual({ borderBlock: 'value1 value2' });
    });
    it('borderBlockColor type', () => {
        expect(typeof cssProps.borderBlockColor).toEqual('function');
    });

    it('borderBlockColor', () => {
        expect(cssProps.borderBlockColor('value1', 'value2')).toEqual({
            borderBlockColor: 'value1 value2'
        });
    });
    it('borderBlockStyle type', () => {
        expect(typeof cssProps.borderBlockStyle).toEqual('function');
    });

    it('borderBlockStyle', () => {
        expect(cssProps.borderBlockStyle('value1', 'value2')).toEqual({
            borderBlockStyle: 'value1 value2'
        });
    });
    it('borderBlockWidth type', () => {
        expect(typeof cssProps.borderBlockWidth).toEqual('function');
    });

    it('borderBlockWidth', () => {
        expect(cssProps.borderBlockWidth('value1', 'value2')).toEqual({
            borderBlockWidth: 'value1 value2'
        });
    });
    it('borderBlockEnd type', () => {
        expect(typeof cssProps.borderBlockEnd).toEqual('function');
    });

    it('borderBlockEnd', () => {
        expect(cssProps.borderBlockEnd('value1', 'value2')).toEqual({
            borderBlockEnd: 'value1 value2'
        });
    });
    it('borderBlockEndColor type', () => {
        expect(typeof cssProps.borderBlockEndColor).toEqual('function');
    });

    it('borderBlockEndColor', () => {
        expect(cssProps.borderBlockEndColor('value1', 'value2')).toEqual({
            borderBlockEndColor: 'value1 value2'
        });
    });
    it('borderBlockEndStyle type', () => {
        expect(typeof cssProps.borderBlockEndStyle).toEqual('function');
    });

    it('borderBlockEndStyle', () => {
        expect(cssProps.borderBlockEndStyle('value1', 'value2')).toEqual({
            borderBlockEndStyle: 'value1 value2'
        });
    });
    it('borderBlockEndWidth type', () => {
        expect(typeof cssProps.borderBlockEndWidth).toEqual('function');
    });

    it('borderBlockEndWidth', () => {
        expect(cssProps.borderBlockEndWidth('value1', 'value2')).toEqual({
            borderBlockEndWidth: 'value1 value2'
        });
    });
    it('borderBlockStart type', () => {
        expect(typeof cssProps.borderBlockStart).toEqual('function');
    });

    it('borderBlockStart', () => {
        expect(cssProps.borderBlockStart('value1', 'value2')).toEqual({
            borderBlockStart: 'value1 value2'
        });
    });
    it('borderBlockStartColor type', () => {
        expect(typeof cssProps.borderBlockStartColor).toEqual('function');
    });

    it('borderBlockStartColor', () => {
        expect(cssProps.borderBlockStartColor('value1', 'value2')).toEqual({
            borderBlockStartColor: 'value1 value2'
        });
    });
    it('borderBlockStartStyle type', () => {
        expect(typeof cssProps.borderBlockStartStyle).toEqual('function');
    });

    it('borderBlockStartStyle', () => {
        expect(cssProps.borderBlockStartStyle('value1', 'value2')).toEqual({
            borderBlockStartStyle: 'value1 value2'
        });
    });
    it('borderBlockStartWidth type', () => {
        expect(typeof cssProps.borderBlockStartWidth).toEqual('function');
    });

    it('borderBlockStartWidth', () => {
        expect(cssProps.borderBlockStartWidth('value1', 'value2')).toEqual({
            borderBlockStartWidth: 'value1 value2'
        });
    });
    it('borderBottom type', () => {
        expect(typeof cssProps.borderBottom).toEqual('function');
    });

    it('borderBottom', () => {
        expect(cssProps.borderBottom('value1', 'value2')).toEqual({
            borderBottom: 'value1 value2'
        });
    });
    it('borderBottomColor type', () => {
        expect(typeof cssProps.borderBottomColor).toEqual('function');
    });

    it('borderBottomColor', () => {
        expect(cssProps.borderBottomColor('value1', 'value2')).toEqual({
            borderBottomColor: 'value1 value2'
        });
    });
    it('borderBottomLeftRadius type', () => {
        expect(typeof cssProps.borderBottomLeftRadius).toEqual('function');
    });

    it('borderBottomLeftRadius', () => {
        expect(cssProps.borderBottomLeftRadius('value1', 'value2')).toEqual({
            borderBottomLeftRadius: 'value1 value2'
        });
    });
    it('borderBottomRightRadius type', () => {
        expect(typeof cssProps.borderBottomRightRadius).toEqual('function');
    });

    it('borderBottomRightRadius', () => {
        expect(cssProps.borderBottomRightRadius('value1', 'value2')).toEqual({
            borderBottomRightRadius: 'value1 value2'
        });
    });
    it('borderBottomStyle type', () => {
        expect(typeof cssProps.borderBottomStyle).toEqual('function');
    });

    it('borderBottomStyle', () => {
        expect(cssProps.borderBottomStyle('value1', 'value2')).toEqual({
            borderBottomStyle: 'value1 value2'
        });
    });
    it('borderBottomWidth type', () => {
        expect(typeof cssProps.borderBottomWidth).toEqual('function');
    });

    it('borderBottomWidth', () => {
        expect(cssProps.borderBottomWidth('value1', 'value2')).toEqual({
            borderBottomWidth: 'value1 value2'
        });
    });
    it('borderCollapse type', () => {
        expect(typeof cssProps.borderCollapse).toEqual('function');
    });

    it('borderCollapse', () => {
        expect(cssProps.borderCollapse('value1', 'value2')).toEqual({
            borderCollapse: 'value1 value2'
        });
    });
    it('borderColor type', () => {
        expect(typeof cssProps.borderColor).toEqual('function');
    });

    it('borderColor', () => {
        expect(cssProps.borderColor('value1', 'value2')).toEqual({ borderColor: 'value1 value2' });
    });
    it('borderEndEndRadius type', () => {
        expect(typeof cssProps.borderEndEndRadius).toEqual('function');
    });

    it('borderEndEndRadius', () => {
        expect(cssProps.borderEndEndRadius('value1', 'value2')).toEqual({
            borderEndEndRadius: 'value1 value2'
        });
    });
    it('borderEndStartRadius type', () => {
        expect(typeof cssProps.borderEndStartRadius).toEqual('function');
    });

    it('borderEndStartRadius', () => {
        expect(cssProps.borderEndStartRadius('value1', 'value2')).toEqual({
            borderEndStartRadius: 'value1 value2'
        });
    });
    it('borderImage type', () => {
        expect(typeof cssProps.borderImage).toEqual('function');
    });

    it('borderImage', () => {
        expect(cssProps.borderImage('value1', 'value2')).toEqual({ borderImage: 'value1 value2' });
    });
    it('borderImageOutset type', () => {
        expect(typeof cssProps.borderImageOutset).toEqual('function');
    });

    it('borderImageOutset', () => {
        expect(cssProps.borderImageOutset('value1', 'value2')).toEqual({
            borderImageOutset: 'value1 value2'
        });
    });
    it('borderImageRepeat type', () => {
        expect(typeof cssProps.borderImageRepeat).toEqual('function');
    });

    it('borderImageRepeat', () => {
        expect(cssProps.borderImageRepeat('value1', 'value2')).toEqual({
            borderImageRepeat: 'value1 value2'
        });
    });
    it('borderImageSlice type', () => {
        expect(typeof cssProps.borderImageSlice).toEqual('function');
    });

    it('borderImageSlice', () => {
        expect(cssProps.borderImageSlice('value1', 'value2')).toEqual({
            borderImageSlice: 'value1 value2'
        });
    });
    it('borderImageSource type', () => {
        expect(typeof cssProps.borderImageSource).toEqual('function');
    });

    it('borderImageSource', () => {
        expect(cssProps.borderImageSource('value1', 'value2')).toEqual({
            borderImageSource: 'value1 value2'
        });
    });
    it('borderImageWidth type', () => {
        expect(typeof cssProps.borderImageWidth).toEqual('function');
    });

    it('borderImageWidth', () => {
        expect(cssProps.borderImageWidth('value1', 'value2')).toEqual({
            borderImageWidth: 'value1 value2'
        });
    });
    it('borderInline type', () => {
        expect(typeof cssProps.borderInline).toEqual('function');
    });

    it('borderInline', () => {
        expect(cssProps.borderInline('value1', 'value2')).toEqual({
            borderInline: 'value1 value2'
        });
    });
    it('borderInlineEnd type', () => {
        expect(typeof cssProps.borderInlineEnd).toEqual('function');
    });

    it('borderInlineEnd', () => {
        expect(cssProps.borderInlineEnd('value1', 'value2')).toEqual({
            borderInlineEnd: 'value1 value2'
        });
    });
    it('borderInlineColor type', () => {
        expect(typeof cssProps.borderInlineColor).toEqual('function');
    });

    it('borderInlineColor', () => {
        expect(cssProps.borderInlineColor('value1', 'value2')).toEqual({
            borderInlineColor: 'value1 value2'
        });
    });
    it('borderInlineStyle type', () => {
        expect(typeof cssProps.borderInlineStyle).toEqual('function');
    });

    it('borderInlineStyle', () => {
        expect(cssProps.borderInlineStyle('value1', 'value2')).toEqual({
            borderInlineStyle: 'value1 value2'
        });
    });
    it('borderInlineWidth type', () => {
        expect(typeof cssProps.borderInlineWidth).toEqual('function');
    });

    it('borderInlineWidth', () => {
        expect(cssProps.borderInlineWidth('value1', 'value2')).toEqual({
            borderInlineWidth: 'value1 value2'
        });
    });
    it('borderInlineEndColor type', () => {
        expect(typeof cssProps.borderInlineEndColor).toEqual('function');
    });

    it('borderInlineEndColor', () => {
        expect(cssProps.borderInlineEndColor('value1', 'value2')).toEqual({
            borderInlineEndColor: 'value1 value2'
        });
    });
    it('borderInlineEndStyle type', () => {
        expect(typeof cssProps.borderInlineEndStyle).toEqual('function');
    });

    it('borderInlineEndStyle', () => {
        expect(cssProps.borderInlineEndStyle('value1', 'value2')).toEqual({
            borderInlineEndStyle: 'value1 value2'
        });
    });
    it('borderInlineEndWidth type', () => {
        expect(typeof cssProps.borderInlineEndWidth).toEqual('function');
    });

    it('borderInlineEndWidth', () => {
        expect(cssProps.borderInlineEndWidth('value1', 'value2')).toEqual({
            borderInlineEndWidth: 'value1 value2'
        });
    });
    it('borderInlineStart type', () => {
        expect(typeof cssProps.borderInlineStart).toEqual('function');
    });

    it('borderInlineStart', () => {
        expect(cssProps.borderInlineStart('value1', 'value2')).toEqual({
            borderInlineStart: 'value1 value2'
        });
    });
    it('borderInlineStartColor type', () => {
        expect(typeof cssProps.borderInlineStartColor).toEqual('function');
    });

    it('borderInlineStartColor', () => {
        expect(cssProps.borderInlineStartColor('value1', 'value2')).toEqual({
            borderInlineStartColor: 'value1 value2'
        });
    });
    it('borderInlineStartStyle type', () => {
        expect(typeof cssProps.borderInlineStartStyle).toEqual('function');
    });

    it('borderInlineStartStyle', () => {
        expect(cssProps.borderInlineStartStyle('value1', 'value2')).toEqual({
            borderInlineStartStyle: 'value1 value2'
        });
    });
    it('borderInlineStartWidth type', () => {
        expect(typeof cssProps.borderInlineStartWidth).toEqual('function');
    });

    it('borderInlineStartWidth', () => {
        expect(cssProps.borderInlineStartWidth('value1', 'value2')).toEqual({
            borderInlineStartWidth: 'value1 value2'
        });
    });
    it('borderLeft type', () => {
        expect(typeof cssProps.borderLeft).toEqual('function');
    });

    it('borderLeft', () => {
        expect(cssProps.borderLeft('value1', 'value2')).toEqual({ borderLeft: 'value1 value2' });
    });
    it('borderLeftColor type', () => {
        expect(typeof cssProps.borderLeftColor).toEqual('function');
    });

    it('borderLeftColor', () => {
        expect(cssProps.borderLeftColor('value1', 'value2')).toEqual({
            borderLeftColor: 'value1 value2'
        });
    });
    it('borderLeftStyle type', () => {
        expect(typeof cssProps.borderLeftStyle).toEqual('function');
    });

    it('borderLeftStyle', () => {
        expect(cssProps.borderLeftStyle('value1', 'value2')).toEqual({
            borderLeftStyle: 'value1 value2'
        });
    });
    it('borderLeftWidth type', () => {
        expect(typeof cssProps.borderLeftWidth).toEqual('function');
    });

    it('borderLeftWidth', () => {
        expect(cssProps.borderLeftWidth('value1', 'value2')).toEqual({
            borderLeftWidth: 'value1 value2'
        });
    });
    it('borderRadius type', () => {
        expect(typeof cssProps.borderRadius).toEqual('function');
    });

    it('borderRadius', () => {
        expect(cssProps.borderRadius('value1', 'value2')).toEqual({
            borderRadius: 'value1 value2'
        });
    });
    it('borderRight type', () => {
        expect(typeof cssProps.borderRight).toEqual('function');
    });

    it('borderRight', () => {
        expect(cssProps.borderRight('value1', 'value2')).toEqual({ borderRight: 'value1 value2' });
    });
    it('borderRightColor type', () => {
        expect(typeof cssProps.borderRightColor).toEqual('function');
    });

    it('borderRightColor', () => {
        expect(cssProps.borderRightColor('value1', 'value2')).toEqual({
            borderRightColor: 'value1 value2'
        });
    });
    it('borderRightStyle type', () => {
        expect(typeof cssProps.borderRightStyle).toEqual('function');
    });

    it('borderRightStyle', () => {
        expect(cssProps.borderRightStyle('value1', 'value2')).toEqual({
            borderRightStyle: 'value1 value2'
        });
    });
    it('borderRightWidth type', () => {
        expect(typeof cssProps.borderRightWidth).toEqual('function');
    });

    it('borderRightWidth', () => {
        expect(cssProps.borderRightWidth('value1', 'value2')).toEqual({
            borderRightWidth: 'value1 value2'
        });
    });
    it('borderSpacing type', () => {
        expect(typeof cssProps.borderSpacing).toEqual('function');
    });

    it('borderSpacing', () => {
        expect(cssProps.borderSpacing('value1', 'value2')).toEqual({
            borderSpacing: 'value1 value2'
        });
    });
    it('borderStartEndRadius type', () => {
        expect(typeof cssProps.borderStartEndRadius).toEqual('function');
    });

    it('borderStartEndRadius', () => {
        expect(cssProps.borderStartEndRadius('value1', 'value2')).toEqual({
            borderStartEndRadius: 'value1 value2'
        });
    });
    it('borderStartStartRadius type', () => {
        expect(typeof cssProps.borderStartStartRadius).toEqual('function');
    });

    it('borderStartStartRadius', () => {
        expect(cssProps.borderStartStartRadius('value1', 'value2')).toEqual({
            borderStartStartRadius: 'value1 value2'
        });
    });
    it('borderStyle type', () => {
        expect(typeof cssProps.borderStyle).toEqual('function');
    });

    it('borderStyle', () => {
        expect(cssProps.borderStyle('value1', 'value2')).toEqual({ borderStyle: 'value1 value2' });
    });
    it('borderTop type', () => {
        expect(typeof cssProps.borderTop).toEqual('function');
    });

    it('borderTop', () => {
        expect(cssProps.borderTop('value1', 'value2')).toEqual({ borderTop: 'value1 value2' });
    });
    it('borderTopColor type', () => {
        expect(typeof cssProps.borderTopColor).toEqual('function');
    });

    it('borderTopColor', () => {
        expect(cssProps.borderTopColor('value1', 'value2')).toEqual({
            borderTopColor: 'value1 value2'
        });
    });
    it('borderTopLeftRadius type', () => {
        expect(typeof cssProps.borderTopLeftRadius).toEqual('function');
    });

    it('borderTopLeftRadius', () => {
        expect(cssProps.borderTopLeftRadius('value1', 'value2')).toEqual({
            borderTopLeftRadius: 'value1 value2'
        });
    });
    it('borderTopRightRadius type', () => {
        expect(typeof cssProps.borderTopRightRadius).toEqual('function');
    });

    it('borderTopRightRadius', () => {
        expect(cssProps.borderTopRightRadius('value1', 'value2')).toEqual({
            borderTopRightRadius: 'value1 value2'
        });
    });
    it('borderTopStyle type', () => {
        expect(typeof cssProps.borderTopStyle).toEqual('function');
    });

    it('borderTopStyle', () => {
        expect(cssProps.borderTopStyle('value1', 'value2')).toEqual({
            borderTopStyle: 'value1 value2'
        });
    });
    it('borderTopWidth type', () => {
        expect(typeof cssProps.borderTopWidth).toEqual('function');
    });

    it('borderTopWidth', () => {
        expect(cssProps.borderTopWidth('value1', 'value2')).toEqual({
            borderTopWidth: 'value1 value2'
        });
    });
    it('borderWidth type', () => {
        expect(typeof cssProps.borderWidth).toEqual('function');
    });

    it('borderWidth', () => {
        expect(cssProps.borderWidth('value1', 'value2')).toEqual({ borderWidth: 'value1 value2' });
    });
    it('bottom type', () => {
        expect(typeof cssProps.bottom).toEqual('function');
    });

    it('bottom', () => {
        expect(cssProps.bottom('value1', 'value2')).toEqual({ bottom: 'value1 value2' });
    });
    it('boxDecorationBreak type', () => {
        expect(typeof cssProps.boxDecorationBreak).toEqual('function');
    });

    it('boxDecorationBreak', () => {
        expect(cssProps.boxDecorationBreak('value1', 'value2')).toEqual({
            boxDecorationBreak: 'value1 value2'
        });
    });
    it('boxShadow type', () => {
        expect(typeof cssProps.boxShadow).toEqual('function');
    });

    it('boxShadow', () => {
        expect(cssProps.boxShadow('value1', 'value2')).toEqual({ boxShadow: 'value1 value2' });
    });
    it('boxSizing type', () => {
        expect(typeof cssProps.boxSizing).toEqual('function');
    });

    it('boxSizing', () => {
        expect(cssProps.boxSizing('value1', 'value2')).toEqual({ boxSizing: 'value1 value2' });
    });
    it('breakAfter type', () => {
        expect(typeof cssProps.breakAfter).toEqual('function');
    });

    it('breakAfter', () => {
        expect(cssProps.breakAfter('value1', 'value2')).toEqual({ breakAfter: 'value1 value2' });
    });
    it('breakBefore type', () => {
        expect(typeof cssProps.breakBefore).toEqual('function');
    });

    it('breakBefore', () => {
        expect(cssProps.breakBefore('value1', 'value2')).toEqual({ breakBefore: 'value1 value2' });
    });
    it('breakInside type', () => {
        expect(typeof cssProps.breakInside).toEqual('function');
    });

    it('breakInside', () => {
        expect(cssProps.breakInside('value1', 'value2')).toEqual({ breakInside: 'value1 value2' });
    });
    it('captionSide type', () => {
        expect(typeof cssProps.captionSide).toEqual('function');
    });

    it('captionSide', () => {
        expect(cssProps.captionSide('value1', 'value2')).toEqual({ captionSide: 'value1 value2' });
    });
    it('caret type', () => {
        expect(typeof cssProps.caret).toEqual('function');
    });

    it('caret', () => {
        expect(cssProps.caret('value1', 'value2')).toEqual({ caret: 'value1 value2' });
    });
    it('caretColor type', () => {
        expect(typeof cssProps.caretColor).toEqual('function');
    });

    it('caretColor', () => {
        expect(cssProps.caretColor('value1', 'value2')).toEqual({ caretColor: 'value1 value2' });
    });
    it('caretShape type', () => {
        expect(typeof cssProps.caretShape).toEqual('function');
    });

    it('caretShape', () => {
        expect(cssProps.caretShape('value1', 'value2')).toEqual({ caretShape: 'value1 value2' });
    });
    it('clear type', () => {
        expect(typeof cssProps.clear).toEqual('function');
    });

    it('clear', () => {
        expect(cssProps.clear('value1', 'value2')).toEqual({ clear: 'value1 value2' });
    });
    it('clip type', () => {
        expect(typeof cssProps.clip).toEqual('function');
    });

    it('clip', () => {
        expect(cssProps.clip('value1', 'value2')).toEqual({ clip: 'value1 value2' });
    });
    it('clipPath type', () => {
        expect(typeof cssProps.clipPath).toEqual('function');
    });

    it('clipPath', () => {
        expect(cssProps.clipPath('value1', 'value2')).toEqual({ clipPath: 'value1 value2' });
    });
    it('color type', () => {
        expect(typeof cssProps.color).toEqual('function');
    });

    it('color', () => {
        expect(cssProps.color('value1', 'value2')).toEqual({ color: 'value1 value2' });
    });
    it('printColorAdjust type', () => {
        expect(typeof cssProps.printColorAdjust).toEqual('function');
    });

    it('printColorAdjust', () => {
        expect(cssProps.printColorAdjust('value1', 'value2')).toEqual({
            printColorAdjust: 'value1 value2'
        });
    });
    it('colorScheme type', () => {
        expect(typeof cssProps.colorScheme).toEqual('function');
    });

    it('colorScheme', () => {
        expect(cssProps.colorScheme('value1', 'value2')).toEqual({ colorScheme: 'value1 value2' });
    });
    it('columnCount type', () => {
        expect(typeof cssProps.columnCount).toEqual('function');
    });

    it('columnCount', () => {
        expect(cssProps.columnCount('value1', 'value2')).toEqual({ columnCount: 'value1 value2' });
    });
    it('columnFill type', () => {
        expect(typeof cssProps.columnFill).toEqual('function');
    });

    it('columnFill', () => {
        expect(cssProps.columnFill('value1', 'value2')).toEqual({ columnFill: 'value1 value2' });
    });
    it('columnGap type', () => {
        expect(typeof cssProps.columnGap).toEqual('function');
    });

    it('columnGap', () => {
        expect(cssProps.columnGap('value1', 'value2')).toEqual({ columnGap: 'value1 value2' });
    });
    it('columnRule type', () => {
        expect(typeof cssProps.columnRule).toEqual('function');
    });

    it('columnRule', () => {
        expect(cssProps.columnRule('value1', 'value2')).toEqual({ columnRule: 'value1 value2' });
    });
    it('columnRuleColor type', () => {
        expect(typeof cssProps.columnRuleColor).toEqual('function');
    });

    it('columnRuleColor', () => {
        expect(cssProps.columnRuleColor('value1', 'value2')).toEqual({
            columnRuleColor: 'value1 value2'
        });
    });
    it('columnRuleStyle type', () => {
        expect(typeof cssProps.columnRuleStyle).toEqual('function');
    });

    it('columnRuleStyle', () => {
        expect(cssProps.columnRuleStyle('value1', 'value2')).toEqual({
            columnRuleStyle: 'value1 value2'
        });
    });
    it('columnRuleWidth type', () => {
        expect(typeof cssProps.columnRuleWidth).toEqual('function');
    });

    it('columnRuleWidth', () => {
        expect(cssProps.columnRuleWidth('value1', 'value2')).toEqual({
            columnRuleWidth: 'value1 value2'
        });
    });
    it('columnSpan type', () => {
        expect(typeof cssProps.columnSpan).toEqual('function');
    });

    it('columnSpan', () => {
        expect(cssProps.columnSpan('value1', 'value2')).toEqual({ columnSpan: 'value1 value2' });
    });
    it('columnWidth type', () => {
        expect(typeof cssProps.columnWidth).toEqual('function');
    });

    it('columnWidth', () => {
        expect(cssProps.columnWidth('value1', 'value2')).toEqual({ columnWidth: 'value1 value2' });
    });
    it('columns type', () => {
        expect(typeof cssProps.columns).toEqual('function');
    });

    it('columns', () => {
        expect(cssProps.columns('value1', 'value2')).toEqual({ columns: 'value1 value2' });
    });
    it('contain type', () => {
        expect(typeof cssProps.contain).toEqual('function');
    });

    it('contain', () => {
        expect(cssProps.contain('value1', 'value2')).toEqual({ contain: 'value1 value2' });
    });
    it('content type', () => {
        expect(typeof cssProps.content).toEqual('function');
    });

    it('content', () => {
        expect(cssProps.content('value1', 'value2')).toEqual({ content: 'value1 value2' });
    });
    it('contentVisibility type', () => {
        expect(typeof cssProps.contentVisibility).toEqual('function');
    });

    it('contentVisibility', () => {
        expect(cssProps.contentVisibility('value1', 'value2')).toEqual({
            contentVisibility: 'value1 value2'
        });
    });
    it('counterIncrement type', () => {
        expect(typeof cssProps.counterIncrement).toEqual('function');
    });

    it('counterIncrement', () => {
        expect(cssProps.counterIncrement('value1', 'value2')).toEqual({
            counterIncrement: 'value1 value2'
        });
    });
    it('counterReset type', () => {
        expect(typeof cssProps.counterReset).toEqual('function');
    });

    it('counterReset', () => {
        expect(cssProps.counterReset('value1', 'value2')).toEqual({
            counterReset: 'value1 value2'
        });
    });
    it('counterSet type', () => {
        expect(typeof cssProps.counterSet).toEqual('function');
    });

    it('counterSet', () => {
        expect(cssProps.counterSet('value1', 'value2')).toEqual({ counterSet: 'value1 value2' });
    });
    it('cursor type', () => {
        expect(typeof cssProps.cursor).toEqual('function');
    });

    it('cursor', () => {
        expect(cssProps.cursor('value1', 'value2')).toEqual({ cursor: 'value1 value2' });
    });
    it('direction type', () => {
        expect(typeof cssProps.direction).toEqual('function');
    });

    it('direction', () => {
        expect(cssProps.direction('value1', 'value2')).toEqual({ direction: 'value1 value2' });
    });
    it('display type', () => {
        expect(typeof cssProps.display).toEqual('function');
    });

    it('display', () => {
        expect(cssProps.display('value1', 'value2')).toEqual({ display: 'value1 value2' });
    });
    it('emptyCells type', () => {
        expect(typeof cssProps.emptyCells).toEqual('function');
    });

    it('emptyCells', () => {
        expect(cssProps.emptyCells('value1', 'value2')).toEqual({ emptyCells: 'value1 value2' });
    });
    it('filter type', () => {
        expect(typeof cssProps.filter).toEqual('function');
    });

    it('filter', () => {
        expect(cssProps.filter('value1', 'value2')).toEqual({ filter: 'value1 value2' });
    });
    it('flex type', () => {
        expect(typeof cssProps.flex).toEqual('function');
    });

    it('flex', () => {
        expect(cssProps.flex('value1', 'value2')).toEqual({ flex: 'value1 value2' });
    });
    it('flexBasis type', () => {
        expect(typeof cssProps.flexBasis).toEqual('function');
    });

    it('flexBasis', () => {
        expect(cssProps.flexBasis('value1', 'value2')).toEqual({ flexBasis: 'value1 value2' });
    });
    it('flexDirection type', () => {
        expect(typeof cssProps.flexDirection).toEqual('function');
    });

    it('flexDirection', () => {
        expect(cssProps.flexDirection('value1', 'value2')).toEqual({
            flexDirection: 'value1 value2'
        });
    });
    it('flexFlow type', () => {
        expect(typeof cssProps.flexFlow).toEqual('function');
    });

    it('flexFlow', () => {
        expect(cssProps.flexFlow('value1', 'value2')).toEqual({ flexFlow: 'value1 value2' });
    });
    it('flexGrow type', () => {
        expect(typeof cssProps.flexGrow).toEqual('function');
    });

    it('flexGrow', () => {
        expect(cssProps.flexGrow('value1', 'value2')).toEqual({ flexGrow: 'value1 value2' });
    });
    it('flexShrink type', () => {
        expect(typeof cssProps.flexShrink).toEqual('function');
    });

    it('flexShrink', () => {
        expect(cssProps.flexShrink('value1', 'value2')).toEqual({ flexShrink: 'value1 value2' });
    });
    it('flexWrap type', () => {
        expect(typeof cssProps.flexWrap).toEqual('function');
    });

    it('flexWrap', () => {
        expect(cssProps.flexWrap('value1', 'value2')).toEqual({ flexWrap: 'value1 value2' });
    });
    it('float type', () => {
        expect(typeof cssProps.float).toEqual('function');
    });

    it('float', () => {
        expect(cssProps.float('value1', 'value2')).toEqual({ float: 'value1 value2' });
    });
    it('font type', () => {
        expect(typeof cssProps.font).toEqual('function');
    });

    it('font', () => {
        expect(cssProps.font('value1', 'value2')).toEqual({ font: 'value1 value2' });
    });
    it('fontFamily type', () => {
        expect(typeof cssProps.fontFamily).toEqual('function');
    });

    it('fontFamily', () => {
        expect(cssProps.fontFamily('value1', 'value2')).toEqual({ fontFamily: 'value1 value2' });
    });
    it('fontFeatureSettings type', () => {
        expect(typeof cssProps.fontFeatureSettings).toEqual('function');
    });

    it('fontFeatureSettings', () => {
        expect(cssProps.fontFeatureSettings('value1', 'value2')).toEqual({
            fontFeatureSettings: 'value1 value2'
        });
    });
    it('fontKerning type', () => {
        expect(typeof cssProps.fontKerning).toEqual('function');
    });

    it('fontKerning', () => {
        expect(cssProps.fontKerning('value1', 'value2')).toEqual({ fontKerning: 'value1 value2' });
    });
    it('fontLanguageOverride type', () => {
        expect(typeof cssProps.fontLanguageOverride).toEqual('function');
    });

    it('fontLanguageOverride', () => {
        expect(cssProps.fontLanguageOverride('value1', 'value2')).toEqual({
            fontLanguageOverride: 'value1 value2'
        });
    });
    it('fontOpticalSizing type', () => {
        expect(typeof cssProps.fontOpticalSizing).toEqual('function');
    });

    it('fontOpticalSizing', () => {
        expect(cssProps.fontOpticalSizing('value1', 'value2')).toEqual({
            fontOpticalSizing: 'value1 value2'
        });
    });
    it('fontVariationSettings type', () => {
        expect(typeof cssProps.fontVariationSettings).toEqual('function');
    });

    it('fontVariationSettings', () => {
        expect(cssProps.fontVariationSettings('value1', 'value2')).toEqual({
            fontVariationSettings: 'value1 value2'
        });
    });
    it('fontSize type', () => {
        expect(typeof cssProps.fontSize).toEqual('function');
    });

    it('fontSize', () => {
        expect(cssProps.fontSize('value1', 'value2')).toEqual({ fontSize: 'value1 value2' });
    });
    it('fontSizeAdjust type', () => {
        expect(typeof cssProps.fontSizeAdjust).toEqual('function');
    });

    it('fontSizeAdjust', () => {
        expect(cssProps.fontSizeAdjust('value1', 'value2')).toEqual({
            fontSizeAdjust: 'value1 value2'
        });
    });
    it('fontStretch type', () => {
        expect(typeof cssProps.fontStretch).toEqual('function');
    });

    it('fontStretch', () => {
        expect(cssProps.fontStretch('value1', 'value2')).toEqual({ fontStretch: 'value1 value2' });
    });
    it('fontStyle type', () => {
        expect(typeof cssProps.fontStyle).toEqual('function');
    });

    it('fontStyle', () => {
        expect(cssProps.fontStyle('value1', 'value2')).toEqual({ fontStyle: 'value1 value2' });
    });
    it('fontSynthesis type', () => {
        expect(typeof cssProps.fontSynthesis).toEqual('function');
    });

    it('fontSynthesis', () => {
        expect(cssProps.fontSynthesis('value1', 'value2')).toEqual({
            fontSynthesis: 'value1 value2'
        });
    });
    it('fontVariant type', () => {
        expect(typeof cssProps.fontVariant).toEqual('function');
    });

    it('fontVariant', () => {
        expect(cssProps.fontVariant('value1', 'value2')).toEqual({ fontVariant: 'value1 value2' });
    });
    it('fontVariantAlternates type', () => {
        expect(typeof cssProps.fontVariantAlternates).toEqual('function');
    });

    it('fontVariantAlternates', () => {
        expect(cssProps.fontVariantAlternates('value1', 'value2')).toEqual({
            fontVariantAlternates: 'value1 value2'
        });
    });
    it('fontVariantCaps type', () => {
        expect(typeof cssProps.fontVariantCaps).toEqual('function');
    });

    it('fontVariantCaps', () => {
        expect(cssProps.fontVariantCaps('value1', 'value2')).toEqual({
            fontVariantCaps: 'value1 value2'
        });
    });
    it('fontVariantEastAsian type', () => {
        expect(typeof cssProps.fontVariantEastAsian).toEqual('function');
    });

    it('fontVariantEastAsian', () => {
        expect(cssProps.fontVariantEastAsian('value1', 'value2')).toEqual({
            fontVariantEastAsian: 'value1 value2'
        });
    });
    it('fontVariantLigatures type', () => {
        expect(typeof cssProps.fontVariantLigatures).toEqual('function');
    });

    it('fontVariantLigatures', () => {
        expect(cssProps.fontVariantLigatures('value1', 'value2')).toEqual({
            fontVariantLigatures: 'value1 value2'
        });
    });
    it('fontVariantNumeric type', () => {
        expect(typeof cssProps.fontVariantNumeric).toEqual('function');
    });

    it('fontVariantNumeric', () => {
        expect(cssProps.fontVariantNumeric('value1', 'value2')).toEqual({
            fontVariantNumeric: 'value1 value2'
        });
    });
    it('fontVariantPosition type', () => {
        expect(typeof cssProps.fontVariantPosition).toEqual('function');
    });

    it('fontVariantPosition', () => {
        expect(cssProps.fontVariantPosition('value1', 'value2')).toEqual({
            fontVariantPosition: 'value1 value2'
        });
    });
    it('fontWeight type', () => {
        expect(typeof cssProps.fontWeight).toEqual('function');
    });

    it('fontWeight', () => {
        expect(cssProps.fontWeight('value1', 'value2')).toEqual({ fontWeight: 'value1 value2' });
    });
    it('gap type', () => {
        expect(typeof cssProps.gap).toEqual('function');
    });

    it('gap', () => {
        expect(cssProps.gap('value1', 'value2')).toEqual({ gap: 'value1 value2' });
    });
    it('grid type', () => {
        expect(typeof cssProps.grid).toEqual('function');
    });

    it('grid', () => {
        expect(cssProps.grid('value1', 'value2')).toEqual({ grid: 'value1 value2' });
    });
    it('gridArea type', () => {
        expect(typeof cssProps.gridArea).toEqual('function');
    });

    it('gridArea', () => {
        expect(cssProps.gridArea('value1', 'value2')).toEqual({ gridArea: 'value1 value2' });
    });
    it('gridAutoColumns type', () => {
        expect(typeof cssProps.gridAutoColumns).toEqual('function');
    });

    it('gridAutoColumns', () => {
        expect(cssProps.gridAutoColumns('value1', 'value2')).toEqual({
            gridAutoColumns: 'value1 value2'
        });
    });
    it('gridAutoFlow type', () => {
        expect(typeof cssProps.gridAutoFlow).toEqual('function');
    });

    it('gridAutoFlow', () => {
        expect(cssProps.gridAutoFlow('value1', 'value2')).toEqual({
            gridAutoFlow: 'value1 value2'
        });
    });
    it('gridAutoRows type', () => {
        expect(typeof cssProps.gridAutoRows).toEqual('function');
    });

    it('gridAutoRows', () => {
        expect(cssProps.gridAutoRows('value1', 'value2')).toEqual({
            gridAutoRows: 'value1 value2'
        });
    });
    it('gridColumn type', () => {
        expect(typeof cssProps.gridColumn).toEqual('function');
    });

    it('gridColumn', () => {
        expect(cssProps.gridColumn('value1', 'value2')).toEqual({ gridColumn: 'value1 value2' });
    });
    it('gridColumnEnd type', () => {
        expect(typeof cssProps.gridColumnEnd).toEqual('function');
    });

    it('gridColumnEnd', () => {
        expect(cssProps.gridColumnEnd('value1', 'value2')).toEqual({
            gridColumnEnd: 'value1 value2'
        });
    });
    it('gridColumnStart type', () => {
        expect(typeof cssProps.gridColumnStart).toEqual('function');
    });

    it('gridColumnStart', () => {
        expect(cssProps.gridColumnStart('value1', 'value2')).toEqual({
            gridColumnStart: 'value1 value2'
        });
    });
    it('gridRow type', () => {
        expect(typeof cssProps.gridRow).toEqual('function');
    });

    it('gridRow', () => {
        expect(cssProps.gridRow('value1', 'value2')).toEqual({ gridRow: 'value1 value2' });
    });
    it('gridRowEnd type', () => {
        expect(typeof cssProps.gridRowEnd).toEqual('function');
    });

    it('gridRowEnd', () => {
        expect(cssProps.gridRowEnd('value1', 'value2')).toEqual({ gridRowEnd: 'value1 value2' });
    });
    it('gridRowStart type', () => {
        expect(typeof cssProps.gridRowStart).toEqual('function');
    });

    it('gridRowStart', () => {
        expect(cssProps.gridRowStart('value1', 'value2')).toEqual({
            gridRowStart: 'value1 value2'
        });
    });
    it('gridTemplate type', () => {
        expect(typeof cssProps.gridTemplate).toEqual('function');
    });

    it('gridTemplate', () => {
        expect(cssProps.gridTemplate('value1', 'value2')).toEqual({
            gridTemplate: 'value1 value2'
        });
    });
    it('gridTemplateAreas type', () => {
        expect(typeof cssProps.gridTemplateAreas).toEqual('function');
    });

    it('gridTemplateAreas', () => {
        expect(cssProps.gridTemplateAreas('value1', 'value2')).toEqual({
            gridTemplateAreas: 'value1 value2'
        });
    });
    it('gridTemplateColumns type', () => {
        expect(typeof cssProps.gridTemplateColumns).toEqual('function');
    });

    it('gridTemplateColumns', () => {
        expect(cssProps.gridTemplateColumns('value1', 'value2')).toEqual({
            gridTemplateColumns: 'value1 value2'
        });
    });
    it('gridTemplateRows type', () => {
        expect(typeof cssProps.gridTemplateRows).toEqual('function');
    });

    it('gridTemplateRows', () => {
        expect(cssProps.gridTemplateRows('value1', 'value2')).toEqual({
            gridTemplateRows: 'value1 value2'
        });
    });
    it('hangingPunctuation type', () => {
        expect(typeof cssProps.hangingPunctuation).toEqual('function');
    });

    it('hangingPunctuation', () => {
        expect(cssProps.hangingPunctuation('value1', 'value2')).toEqual({
            hangingPunctuation: 'value1 value2'
        });
    });
    it('height type', () => {
        expect(typeof cssProps.height).toEqual('function');
    });

    it('height', () => {
        expect(cssProps.height('value1', 'value2')).toEqual({ height: 'value1 value2' });
    });
    it('hyphenateCharacter type', () => {
        expect(typeof cssProps.hyphenateCharacter).toEqual('function');
    });

    it('hyphenateCharacter', () => {
        expect(cssProps.hyphenateCharacter('value1', 'value2')).toEqual({
            hyphenateCharacter: 'value1 value2'
        });
    });
    it('hyphenateLimitChars type', () => {
        expect(typeof cssProps.hyphenateLimitChars).toEqual('function');
    });

    it('hyphenateLimitChars', () => {
        expect(cssProps.hyphenateLimitChars('value1', 'value2')).toEqual({
            hyphenateLimitChars: 'value1 value2'
        });
    });
    it('hyphens type', () => {
        expect(typeof cssProps.hyphens).toEqual('function');
    });

    it('hyphens', () => {
        expect(cssProps.hyphens('value1', 'value2')).toEqual({ hyphens: 'value1 value2' });
    });
    it('imageOrientation type', () => {
        expect(typeof cssProps.imageOrientation).toEqual('function');
    });

    it('imageOrientation', () => {
        expect(cssProps.imageOrientation('value1', 'value2')).toEqual({
            imageOrientation: 'value1 value2'
        });
    });
    it('imageRendering type', () => {
        expect(typeof cssProps.imageRendering).toEqual('function');
    });

    it('imageRendering', () => {
        expect(cssProps.imageRendering('value1', 'value2')).toEqual({
            imageRendering: 'value1 value2'
        });
    });
    it('inlineSize type', () => {
        expect(typeof cssProps.inlineSize).toEqual('function');
    });

    it('inlineSize', () => {
        expect(cssProps.inlineSize('value1', 'value2')).toEqual({ inlineSize: 'value1 value2' });
    });
    it('inputSecurity type', () => {
        expect(typeof cssProps.inputSecurity).toEqual('function');
    });

    it('inputSecurity', () => {
        expect(cssProps.inputSecurity('value1', 'value2')).toEqual({
            inputSecurity: 'value1 value2'
        });
    });
    it('inset type', () => {
        expect(typeof cssProps.inset).toEqual('function');
    });

    it('inset', () => {
        expect(cssProps.inset('value1', 'value2')).toEqual({ inset: 'value1 value2' });
    });
    it('insetBlock type', () => {
        expect(typeof cssProps.insetBlock).toEqual('function');
    });

    it('insetBlock', () => {
        expect(cssProps.insetBlock('value1', 'value2')).toEqual({ insetBlock: 'value1 value2' });
    });
    it('insetBlockEnd type', () => {
        expect(typeof cssProps.insetBlockEnd).toEqual('function');
    });

    it('insetBlockEnd', () => {
        expect(cssProps.insetBlockEnd('value1', 'value2')).toEqual({
            insetBlockEnd: 'value1 value2'
        });
    });
    it('insetBlockStart type', () => {
        expect(typeof cssProps.insetBlockStart).toEqual('function');
    });

    it('insetBlockStart', () => {
        expect(cssProps.insetBlockStart('value1', 'value2')).toEqual({
            insetBlockStart: 'value1 value2'
        });
    });
    it('insetInline type', () => {
        expect(typeof cssProps.insetInline).toEqual('function');
    });

    it('insetInline', () => {
        expect(cssProps.insetInline('value1', 'value2')).toEqual({ insetInline: 'value1 value2' });
    });
    it('insetInlineEnd type', () => {
        expect(typeof cssProps.insetInlineEnd).toEqual('function');
    });

    it('insetInlineEnd', () => {
        expect(cssProps.insetInlineEnd('value1', 'value2')).toEqual({
            insetInlineEnd: 'value1 value2'
        });
    });
    it('insetInlineStart type', () => {
        expect(typeof cssProps.insetInlineStart).toEqual('function');
    });

    it('insetInlineStart', () => {
        expect(cssProps.insetInlineStart('value1', 'value2')).toEqual({
            insetInlineStart: 'value1 value2'
        });
    });
    it('isolation type', () => {
        expect(typeof cssProps.isolation).toEqual('function');
    });

    it('isolation', () => {
        expect(cssProps.isolation('value1', 'value2')).toEqual({ isolation: 'value1 value2' });
    });
    it('justifyContent type', () => {
        expect(typeof cssProps.justifyContent).toEqual('function');
    });

    it('justifyContent', () => {
        expect(cssProps.justifyContent('value1', 'value2')).toEqual({
            justifyContent: 'value1 value2'
        });
    });
    it('justifyItems type', () => {
        expect(typeof cssProps.justifyItems).toEqual('function');
    });

    it('justifyItems', () => {
        expect(cssProps.justifyItems('value1', 'value2')).toEqual({
            justifyItems: 'value1 value2'
        });
    });
    it('justifySelf type', () => {
        expect(typeof cssProps.justifySelf).toEqual('function');
    });

    it('justifySelf', () => {
        expect(cssProps.justifySelf('value1', 'value2')).toEqual({ justifySelf: 'value1 value2' });
    });
    it('left type', () => {
        expect(typeof cssProps.left).toEqual('function');
    });

    it('left', () => {
        expect(cssProps.left('value1', 'value2')).toEqual({ left: 'value1 value2' });
    });
    it('letterSpacing type', () => {
        expect(typeof cssProps.letterSpacing).toEqual('function');
    });

    it('letterSpacing', () => {
        expect(cssProps.letterSpacing('value1', 'value2')).toEqual({
            letterSpacing: 'value1 value2'
        });
    });
    it('lineBreak type', () => {
        expect(typeof cssProps.lineBreak).toEqual('function');
    });

    it('lineBreak', () => {
        expect(cssProps.lineBreak('value1', 'value2')).toEqual({ lineBreak: 'value1 value2' });
    });
    it('lineHeight type', () => {
        expect(typeof cssProps.lineHeight).toEqual('function');
    });

    it('lineHeight', () => {
        expect(cssProps.lineHeight('value1', 'value2')).toEqual({ lineHeight: 'value1 value2' });
    });
    it('listStyle type', () => {
        expect(typeof cssProps.listStyle).toEqual('function');
    });

    it('listStyle', () => {
        expect(cssProps.listStyle('value1', 'value2')).toEqual({ listStyle: 'value1 value2' });
    });
    it('listStyleImage type', () => {
        expect(typeof cssProps.listStyleImage).toEqual('function');
    });

    it('listStyleImage', () => {
        expect(cssProps.listStyleImage('value1', 'value2')).toEqual({
            listStyleImage: 'value1 value2'
        });
    });
    it('listStylePosition type', () => {
        expect(typeof cssProps.listStylePosition).toEqual('function');
    });

    it('listStylePosition', () => {
        expect(cssProps.listStylePosition('value1', 'value2')).toEqual({
            listStylePosition: 'value1 value2'
        });
    });
    it('listStyleType type', () => {
        expect(typeof cssProps.listStyleType).toEqual('function');
    });

    it('listStyleType', () => {
        expect(cssProps.listStyleType('value1', 'value2')).toEqual({
            listStyleType: 'value1 value2'
        });
    });
    it('margin type', () => {
        expect(typeof cssProps.margin).toEqual('function');
    });

    it('margin', () => {
        expect(cssProps.margin('value1', 'value2')).toEqual({ margin: 'value1 value2' });
    });
    it('marginBlock type', () => {
        expect(typeof cssProps.marginBlock).toEqual('function');
    });

    it('marginBlock', () => {
        expect(cssProps.marginBlock('value1', 'value2')).toEqual({ marginBlock: 'value1 value2' });
    });
    it('marginBlockEnd type', () => {
        expect(typeof cssProps.marginBlockEnd).toEqual('function');
    });

    it('marginBlockEnd', () => {
        expect(cssProps.marginBlockEnd('value1', 'value2')).toEqual({
            marginBlockEnd: 'value1 value2'
        });
    });
    it('marginBlockStart type', () => {
        expect(typeof cssProps.marginBlockStart).toEqual('function');
    });

    it('marginBlockStart', () => {
        expect(cssProps.marginBlockStart('value1', 'value2')).toEqual({
            marginBlockStart: 'value1 value2'
        });
    });
    it('marginBottom type', () => {
        expect(typeof cssProps.marginBottom).toEqual('function');
    });

    it('marginBottom', () => {
        expect(cssProps.marginBottom('value1', 'value2')).toEqual({
            marginBottom: 'value1 value2'
        });
    });
    it('marginInline type', () => {
        expect(typeof cssProps.marginInline).toEqual('function');
    });

    it('marginInline', () => {
        expect(cssProps.marginInline('value1', 'value2')).toEqual({
            marginInline: 'value1 value2'
        });
    });
    it('marginInlineEnd type', () => {
        expect(typeof cssProps.marginInlineEnd).toEqual('function');
    });

    it('marginInlineEnd', () => {
        expect(cssProps.marginInlineEnd('value1', 'value2')).toEqual({
            marginInlineEnd: 'value1 value2'
        });
    });
    it('marginInlineStart type', () => {
        expect(typeof cssProps.marginInlineStart).toEqual('function');
    });

    it('marginInlineStart', () => {
        expect(cssProps.marginInlineStart('value1', 'value2')).toEqual({
            marginInlineStart: 'value1 value2'
        });
    });
    it('marginLeft type', () => {
        expect(typeof cssProps.marginLeft).toEqual('function');
    });

    it('marginLeft', () => {
        expect(cssProps.marginLeft('value1', 'value2')).toEqual({ marginLeft: 'value1 value2' });
    });
    it('marginRight type', () => {
        expect(typeof cssProps.marginRight).toEqual('function');
    });

    it('marginRight', () => {
        expect(cssProps.marginRight('value1', 'value2')).toEqual({ marginRight: 'value1 value2' });
    });
    it('marginTop type', () => {
        expect(typeof cssProps.marginTop).toEqual('function');
    });

    it('marginTop', () => {
        expect(cssProps.marginTop('value1', 'value2')).toEqual({ marginTop: 'value1 value2' });
    });
    it('mask type', () => {
        expect(typeof cssProps.mask).toEqual('function');
    });

    it('mask', () => {
        expect(cssProps.mask('value1', 'value2')).toEqual({ mask: 'value1 value2' });
    });
    it('maskBorder type', () => {
        expect(typeof cssProps.maskBorder).toEqual('function');
    });

    it('maskBorder', () => {
        expect(cssProps.maskBorder('value1', 'value2')).toEqual({ maskBorder: 'value1 value2' });
    });
    it('maskBorderMode type', () => {
        expect(typeof cssProps.maskBorderMode).toEqual('function');
    });

    it('maskBorderMode', () => {
        expect(cssProps.maskBorderMode('value1', 'value2')).toEqual({
            maskBorderMode: 'value1 value2'
        });
    });
    it('maskBorderOutset type', () => {
        expect(typeof cssProps.maskBorderOutset).toEqual('function');
    });

    it('maskBorderOutset', () => {
        expect(cssProps.maskBorderOutset('value1', 'value2')).toEqual({
            maskBorderOutset: 'value1 value2'
        });
    });
    it('maskBorderRepeat type', () => {
        expect(typeof cssProps.maskBorderRepeat).toEqual('function');
    });

    it('maskBorderRepeat', () => {
        expect(cssProps.maskBorderRepeat('value1', 'value2')).toEqual({
            maskBorderRepeat: 'value1 value2'
        });
    });
    it('maskBorderSlice type', () => {
        expect(typeof cssProps.maskBorderSlice).toEqual('function');
    });

    it('maskBorderSlice', () => {
        expect(cssProps.maskBorderSlice('value1', 'value2')).toEqual({
            maskBorderSlice: 'value1 value2'
        });
    });
    it('maskBorderSource type', () => {
        expect(typeof cssProps.maskBorderSource).toEqual('function');
    });

    it('maskBorderSource', () => {
        expect(cssProps.maskBorderSource('value1', 'value2')).toEqual({
            maskBorderSource: 'value1 value2'
        });
    });
    it('maskBorderWidth type', () => {
        expect(typeof cssProps.maskBorderWidth).toEqual('function');
    });

    it('maskBorderWidth', () => {
        expect(cssProps.maskBorderWidth('value1', 'value2')).toEqual({
            maskBorderWidth: 'value1 value2'
        });
    });
    it('maskClip type', () => {
        expect(typeof cssProps.maskClip).toEqual('function');
    });

    it('maskClip', () => {
        expect(cssProps.maskClip('value1', 'value2')).toEqual({ maskClip: 'value1 value2' });
    });
    it('maskComposite type', () => {
        expect(typeof cssProps.maskComposite).toEqual('function');
    });

    it('maskComposite', () => {
        expect(cssProps.maskComposite('value1', 'value2')).toEqual({
            maskComposite: 'value1 value2'
        });
    });
    it('maskImage type', () => {
        expect(typeof cssProps.maskImage).toEqual('function');
    });

    it('maskImage', () => {
        expect(cssProps.maskImage('value1', 'value2')).toEqual({ maskImage: 'value1 value2' });
    });
    it('maskMode type', () => {
        expect(typeof cssProps.maskMode).toEqual('function');
    });

    it('maskMode', () => {
        expect(cssProps.maskMode('value1', 'value2')).toEqual({ maskMode: 'value1 value2' });
    });
    it('maskOrigin type', () => {
        expect(typeof cssProps.maskOrigin).toEqual('function');
    });

    it('maskOrigin', () => {
        expect(cssProps.maskOrigin('value1', 'value2')).toEqual({ maskOrigin: 'value1 value2' });
    });
    it('maskPosition type', () => {
        expect(typeof cssProps.maskPosition).toEqual('function');
    });

    it('maskPosition', () => {
        expect(cssProps.maskPosition('value1', 'value2')).toEqual({
            maskPosition: 'value1 value2'
        });
    });
    it('maskRepeat type', () => {
        expect(typeof cssProps.maskRepeat).toEqual('function');
    });

    it('maskRepeat', () => {
        expect(cssProps.maskRepeat('value1', 'value2')).toEqual({ maskRepeat: 'value1 value2' });
    });
    it('maskSize type', () => {
        expect(typeof cssProps.maskSize).toEqual('function');
    });

    it('maskSize', () => {
        expect(cssProps.maskSize('value1', 'value2')).toEqual({ maskSize: 'value1 value2' });
    });
    it('maskType type', () => {
        expect(typeof cssProps.maskType).toEqual('function');
    });

    it('maskType', () => {
        expect(cssProps.maskType('value1', 'value2')).toEqual({ maskType: 'value1 value2' });
    });
    it('mathDepth type', () => {
        expect(typeof cssProps.mathDepth).toEqual('function');
    });

    it('mathDepth', () => {
        expect(cssProps.mathDepth('value1', 'value2')).toEqual({ mathDepth: 'value1 value2' });
    });
    it('mathShift type', () => {
        expect(typeof cssProps.mathShift).toEqual('function');
    });

    it('mathShift', () => {
        expect(cssProps.mathShift('value1', 'value2')).toEqual({ mathShift: 'value1 value2' });
    });
    it('mathStyle type', () => {
        expect(typeof cssProps.mathStyle).toEqual('function');
    });

    it('mathStyle', () => {
        expect(cssProps.mathStyle('value1', 'value2')).toEqual({ mathStyle: 'value1 value2' });
    });
    it('maxBlockSize type', () => {
        expect(typeof cssProps.maxBlockSize).toEqual('function');
    });

    it('maxBlockSize', () => {
        expect(cssProps.maxBlockSize('value1', 'value2')).toEqual({
            maxBlockSize: 'value1 value2'
        });
    });
    it('maxHeight type', () => {
        expect(typeof cssProps.maxHeight).toEqual('function');
    });

    it('maxHeight', () => {
        expect(cssProps.maxHeight('value1', 'value2')).toEqual({ maxHeight: 'value1 value2' });
    });
    it('maxInlineSize type', () => {
        expect(typeof cssProps.maxInlineSize).toEqual('function');
    });

    it('maxInlineSize', () => {
        expect(cssProps.maxInlineSize('value1', 'value2')).toEqual({
            maxInlineSize: 'value1 value2'
        });
    });
    it('maxWidth type', () => {
        expect(typeof cssProps.maxWidth).toEqual('function');
    });

    it('maxWidth', () => {
        expect(cssProps.maxWidth('value1', 'value2')).toEqual({ maxWidth: 'value1 value2' });
    });
    it('minBlockSize type', () => {
        expect(typeof cssProps.minBlockSize).toEqual('function');
    });

    it('minBlockSize', () => {
        expect(cssProps.minBlockSize('value1', 'value2')).toEqual({
            minBlockSize: 'value1 value2'
        });
    });
    it('minHeight type', () => {
        expect(typeof cssProps.minHeight).toEqual('function');
    });

    it('minHeight', () => {
        expect(cssProps.minHeight('value1', 'value2')).toEqual({ minHeight: 'value1 value2' });
    });
    it('minInlineSize type', () => {
        expect(typeof cssProps.minInlineSize).toEqual('function');
    });

    it('minInlineSize', () => {
        expect(cssProps.minInlineSize('value1', 'value2')).toEqual({
            minInlineSize: 'value1 value2'
        });
    });
    it('minWidth type', () => {
        expect(typeof cssProps.minWidth).toEqual('function');
    });

    it('minWidth', () => {
        expect(cssProps.minWidth('value1', 'value2')).toEqual({ minWidth: 'value1 value2' });
    });
    it('mixBlendMode type', () => {
        expect(typeof cssProps.mixBlendMode).toEqual('function');
    });

    it('mixBlendMode', () => {
        expect(cssProps.mixBlendMode('value1', 'value2')).toEqual({
            mixBlendMode: 'value1 value2'
        });
    });
    it('objectFit type', () => {
        expect(typeof cssProps.objectFit).toEqual('function');
    });

    it('objectFit', () => {
        expect(cssProps.objectFit('value1', 'value2')).toEqual({ objectFit: 'value1 value2' });
    });
    it('objectPosition type', () => {
        expect(typeof cssProps.objectPosition).toEqual('function');
    });

    it('objectPosition', () => {
        expect(cssProps.objectPosition('value1', 'value2')).toEqual({
            objectPosition: 'value1 value2'
        });
    });
    it('offset type', () => {
        expect(typeof cssProps.offset).toEqual('function');
    });

    it('offset', () => {
        expect(cssProps.offset('value1', 'value2')).toEqual({ offset: 'value1 value2' });
    });
    it('offsetAnchor type', () => {
        expect(typeof cssProps.offsetAnchor).toEqual('function');
    });

    it('offsetAnchor', () => {
        expect(cssProps.offsetAnchor('value1', 'value2')).toEqual({
            offsetAnchor: 'value1 value2'
        });
    });
    it('offsetDistance type', () => {
        expect(typeof cssProps.offsetDistance).toEqual('function');
    });

    it('offsetDistance', () => {
        expect(cssProps.offsetDistance('value1', 'value2')).toEqual({
            offsetDistance: 'value1 value2'
        });
    });
    it('offsetPath type', () => {
        expect(typeof cssProps.offsetPath).toEqual('function');
    });

    it('offsetPath', () => {
        expect(cssProps.offsetPath('value1', 'value2')).toEqual({ offsetPath: 'value1 value2' });
    });
    it('offsetRotate type', () => {
        expect(typeof cssProps.offsetRotate).toEqual('function');
    });

    it('offsetRotate', () => {
        expect(cssProps.offsetRotate('value1', 'value2')).toEqual({
            offsetRotate: 'value1 value2'
        });
    });
    it('opacity type', () => {
        expect(typeof cssProps.opacity).toEqual('function');
    });

    it('opacity', () => {
        expect(cssProps.opacity('value1', 'value2')).toEqual({ opacity: 'value1 value2' });
    });
    it('order type', () => {
        expect(typeof cssProps.order).toEqual('function');
    });

    it('order', () => {
        expect(cssProps.order('value1', 'value2')).toEqual({ order: 'value1 value2' });
    });
    it('orphans type', () => {
        expect(typeof cssProps.orphans).toEqual('function');
    });

    it('orphans', () => {
        expect(cssProps.orphans('value1', 'value2')).toEqual({ orphans: 'value1 value2' });
    });
    it('outline type', () => {
        expect(typeof cssProps.outline).toEqual('function');
    });

    it('outline', () => {
        expect(cssProps.outline('value1', 'value2')).toEqual({ outline: 'value1 value2' });
    });
    it('outlineColor type', () => {
        expect(typeof cssProps.outlineColor).toEqual('function');
    });

    it('outlineColor', () => {
        expect(cssProps.outlineColor('value1', 'value2')).toEqual({
            outlineColor: 'value1 value2'
        });
    });
    it('outlineOffset type', () => {
        expect(typeof cssProps.outlineOffset).toEqual('function');
    });

    it('outlineOffset', () => {
        expect(cssProps.outlineOffset('value1', 'value2')).toEqual({
            outlineOffset: 'value1 value2'
        });
    });
    it('outlineStyle type', () => {
        expect(typeof cssProps.outlineStyle).toEqual('function');
    });

    it('outlineStyle', () => {
        expect(cssProps.outlineStyle('value1', 'value2')).toEqual({
            outlineStyle: 'value1 value2'
        });
    });
    it('outlineWidth type', () => {
        expect(typeof cssProps.outlineWidth).toEqual('function');
    });

    it('outlineWidth', () => {
        expect(cssProps.outlineWidth('value1', 'value2')).toEqual({
            outlineWidth: 'value1 value2'
        });
    });
    it('overflow type', () => {
        expect(typeof cssProps.overflow).toEqual('function');
    });

    it('overflow', () => {
        expect(cssProps.overflow('value1', 'value2')).toEqual({ overflow: 'value1 value2' });
    });
    it('overflowAnchor type', () => {
        expect(typeof cssProps.overflowAnchor).toEqual('function');
    });

    it('overflowAnchor', () => {
        expect(cssProps.overflowAnchor('value1', 'value2')).toEqual({
            overflowAnchor: 'value1 value2'
        });
    });
    it('overflowBlock type', () => {
        expect(typeof cssProps.overflowBlock).toEqual('function');
    });

    it('overflowBlock', () => {
        expect(cssProps.overflowBlock('value1', 'value2')).toEqual({
            overflowBlock: 'value1 value2'
        });
    });
    it('overflowClipMargin type', () => {
        expect(typeof cssProps.overflowClipMargin).toEqual('function');
    });

    it('overflowClipMargin', () => {
        expect(cssProps.overflowClipMargin('value1', 'value2')).toEqual({
            overflowClipMargin: 'value1 value2'
        });
    });
    it('overflowInline type', () => {
        expect(typeof cssProps.overflowInline).toEqual('function');
    });

    it('overflowInline', () => {
        expect(cssProps.overflowInline('value1', 'value2')).toEqual({
            overflowInline: 'value1 value2'
        });
    });
    it('overflowWrap type', () => {
        expect(typeof cssProps.overflowWrap).toEqual('function');
    });

    it('overflowWrap', () => {
        expect(cssProps.overflowWrap('value1', 'value2')).toEqual({
            overflowWrap: 'value1 value2'
        });
    });
    it('overflowX type', () => {
        expect(typeof cssProps.overflowX).toEqual('function');
    });

    it('overflowX', () => {
        expect(cssProps.overflowX('value1', 'value2')).toEqual({ overflowX: 'value1 value2' });
    });
    it('overflowY type', () => {
        expect(typeof cssProps.overflowY).toEqual('function');
    });

    it('overflowY', () => {
        expect(cssProps.overflowY('value1', 'value2')).toEqual({ overflowY: 'value1 value2' });
    });
    it('overscrollBehavior type', () => {
        expect(typeof cssProps.overscrollBehavior).toEqual('function');
    });

    it('overscrollBehavior', () => {
        expect(cssProps.overscrollBehavior('value1', 'value2')).toEqual({
            overscrollBehavior: 'value1 value2'
        });
    });
    it('overscrollBehaviorBlock type', () => {
        expect(typeof cssProps.overscrollBehaviorBlock).toEqual('function');
    });

    it('overscrollBehaviorBlock', () => {
        expect(cssProps.overscrollBehaviorBlock('value1', 'value2')).toEqual({
            overscrollBehaviorBlock: 'value1 value2'
        });
    });
    it('overscrollBehaviorInline type', () => {
        expect(typeof cssProps.overscrollBehaviorInline).toEqual('function');
    });

    it('overscrollBehaviorInline', () => {
        expect(cssProps.overscrollBehaviorInline('value1', 'value2')).toEqual({
            overscrollBehaviorInline: 'value1 value2'
        });
    });
    it('overscrollBehaviorX type', () => {
        expect(typeof cssProps.overscrollBehaviorX).toEqual('function');
    });

    it('overscrollBehaviorX', () => {
        expect(cssProps.overscrollBehaviorX('value1', 'value2')).toEqual({
            overscrollBehaviorX: 'value1 value2'
        });
    });
    it('overscrollBehaviorY type', () => {
        expect(typeof cssProps.overscrollBehaviorY).toEqual('function');
    });

    it('overscrollBehaviorY', () => {
        expect(cssProps.overscrollBehaviorY('value1', 'value2')).toEqual({
            overscrollBehaviorY: 'value1 value2'
        });
    });
    it('padding type', () => {
        expect(typeof cssProps.padding).toEqual('function');
    });

    it('padding', () => {
        expect(cssProps.padding('value1', 'value2')).toEqual({ padding: 'value1 value2' });
    });
    it('paddingBlock type', () => {
        expect(typeof cssProps.paddingBlock).toEqual('function');
    });

    it('paddingBlock', () => {
        expect(cssProps.paddingBlock('value1', 'value2')).toEqual({
            paddingBlock: 'value1 value2'
        });
    });
    it('paddingBlockEnd type', () => {
        expect(typeof cssProps.paddingBlockEnd).toEqual('function');
    });

    it('paddingBlockEnd', () => {
        expect(cssProps.paddingBlockEnd('value1', 'value2')).toEqual({
            paddingBlockEnd: 'value1 value2'
        });
    });
    it('paddingBlockStart type', () => {
        expect(typeof cssProps.paddingBlockStart).toEqual('function');
    });

    it('paddingBlockStart', () => {
        expect(cssProps.paddingBlockStart('value1', 'value2')).toEqual({
            paddingBlockStart: 'value1 value2'
        });
    });
    it('paddingBottom type', () => {
        expect(typeof cssProps.paddingBottom).toEqual('function');
    });

    it('paddingBottom', () => {
        expect(cssProps.paddingBottom('value1', 'value2')).toEqual({
            paddingBottom: 'value1 value2'
        });
    });
    it('paddingInline type', () => {
        expect(typeof cssProps.paddingInline).toEqual('function');
    });

    it('paddingInline', () => {
        expect(cssProps.paddingInline('value1', 'value2')).toEqual({
            paddingInline: 'value1 value2'
        });
    });
    it('paddingInlineEnd type', () => {
        expect(typeof cssProps.paddingInlineEnd).toEqual('function');
    });

    it('paddingInlineEnd', () => {
        expect(cssProps.paddingInlineEnd('value1', 'value2')).toEqual({
            paddingInlineEnd: 'value1 value2'
        });
    });
    it('paddingInlineStart type', () => {
        expect(typeof cssProps.paddingInlineStart).toEqual('function');
    });

    it('paddingInlineStart', () => {
        expect(cssProps.paddingInlineStart('value1', 'value2')).toEqual({
            paddingInlineStart: 'value1 value2'
        });
    });
    it('paddingLeft type', () => {
        expect(typeof cssProps.paddingLeft).toEqual('function');
    });

    it('paddingLeft', () => {
        expect(cssProps.paddingLeft('value1', 'value2')).toEqual({ paddingLeft: 'value1 value2' });
    });
    it('paddingRight type', () => {
        expect(typeof cssProps.paddingRight).toEqual('function');
    });

    it('paddingRight', () => {
        expect(cssProps.paddingRight('value1', 'value2')).toEqual({
            paddingRight: 'value1 value2'
        });
    });
    it('paddingTop type', () => {
        expect(typeof cssProps.paddingTop).toEqual('function');
    });

    it('paddingTop', () => {
        expect(cssProps.paddingTop('value1', 'value2')).toEqual({ paddingTop: 'value1 value2' });
    });
    it('pageBreakAfter type', () => {
        expect(typeof cssProps.pageBreakAfter).toEqual('function');
    });

    it('pageBreakAfter', () => {
        expect(cssProps.pageBreakAfter('value1', 'value2')).toEqual({
            pageBreakAfter: 'value1 value2'
        });
    });
    it('pageBreakBefore type', () => {
        expect(typeof cssProps.pageBreakBefore).toEqual('function');
    });

    it('pageBreakBefore', () => {
        expect(cssProps.pageBreakBefore('value1', 'value2')).toEqual({
            pageBreakBefore: 'value1 value2'
        });
    });
    it('pageBreakInside type', () => {
        expect(typeof cssProps.pageBreakInside).toEqual('function');
    });

    it('pageBreakInside', () => {
        expect(cssProps.pageBreakInside('value1', 'value2')).toEqual({
            pageBreakInside: 'value1 value2'
        });
    });
    it('paintOrder type', () => {
        expect(typeof cssProps.paintOrder).toEqual('function');
    });

    it('paintOrder', () => {
        expect(cssProps.paintOrder('value1', 'value2')).toEqual({ paintOrder: 'value1 value2' });
    });
    it('perspective type', () => {
        expect(typeof cssProps.perspective).toEqual('function');
    });

    it('perspective', () => {
        expect(cssProps.perspective('value1', 'value2')).toEqual({ perspective: 'value1 value2' });
    });
    it('perspectiveOrigin type', () => {
        expect(typeof cssProps.perspectiveOrigin).toEqual('function');
    });

    it('perspectiveOrigin', () => {
        expect(cssProps.perspectiveOrigin('value1', 'value2')).toEqual({
            perspectiveOrigin: 'value1 value2'
        });
    });
    it('placeContent type', () => {
        expect(typeof cssProps.placeContent).toEqual('function');
    });

    it('placeContent', () => {
        expect(cssProps.placeContent('value1', 'value2')).toEqual({
            placeContent: 'value1 value2'
        });
    });
    it('placeItems type', () => {
        expect(typeof cssProps.placeItems).toEqual('function');
    });

    it('placeItems', () => {
        expect(cssProps.placeItems('value1', 'value2')).toEqual({ placeItems: 'value1 value2' });
    });
    it('placeSelf type', () => {
        expect(typeof cssProps.placeSelf).toEqual('function');
    });

    it('placeSelf', () => {
        expect(cssProps.placeSelf('value1', 'value2')).toEqual({ placeSelf: 'value1 value2' });
    });
    it('pointerEvents type', () => {
        expect(typeof cssProps.pointerEvents).toEqual('function');
    });

    it('pointerEvents', () => {
        expect(cssProps.pointerEvents('value1', 'value2')).toEqual({
            pointerEvents: 'value1 value2'
        });
    });
    it('position type', () => {
        expect(typeof cssProps.position).toEqual('function');
    });

    it('position', () => {
        expect(cssProps.position('value1', 'value2')).toEqual({ position: 'value1 value2' });
    });
    it('quotes type', () => {
        expect(typeof cssProps.quotes).toEqual('function');
    });

    it('quotes', () => {
        expect(cssProps.quotes('value1', 'value2')).toEqual({ quotes: 'value1 value2' });
    });
    it('resize type', () => {
        expect(typeof cssProps.resize).toEqual('function');
    });

    it('resize', () => {
        expect(cssProps.resize('value1', 'value2')).toEqual({ resize: 'value1 value2' });
    });
    it('right type', () => {
        expect(typeof cssProps.right).toEqual('function');
    });

    it('right', () => {
        expect(cssProps.right('value1', 'value2')).toEqual({ right: 'value1 value2' });
    });
    it('rotate type', () => {
        expect(typeof cssProps.rotate).toEqual('function');
    });

    it('rotate', () => {
        expect(cssProps.rotate('value1', 'value2')).toEqual({ rotate: 'value1 value2' });
    });
    it('rowGap type', () => {
        expect(typeof cssProps.rowGap).toEqual('function');
    });

    it('rowGap', () => {
        expect(cssProps.rowGap('value1', 'value2')).toEqual({ rowGap: 'value1 value2' });
    });
    it('scale type', () => {
        expect(typeof cssProps.scale).toEqual('function');
    });

    it('scale', () => {
        expect(cssProps.scale('value1', 'value2')).toEqual({ scale: 'value1 value2' });
    });
    it('scrollbarColor type', () => {
        expect(typeof cssProps.scrollbarColor).toEqual('function');
    });

    it('scrollbarColor', () => {
        expect(cssProps.scrollbarColor('value1', 'value2')).toEqual({
            scrollbarColor: 'value1 value2'
        });
    });
    it('scrollbarGutter type', () => {
        expect(typeof cssProps.scrollbarGutter).toEqual('function');
    });

    it('scrollbarGutter', () => {
        expect(cssProps.scrollbarGutter('value1', 'value2')).toEqual({
            scrollbarGutter: 'value1 value2'
        });
    });
    it('scrollbarWidth type', () => {
        expect(typeof cssProps.scrollbarWidth).toEqual('function');
    });

    it('scrollbarWidth', () => {
        expect(cssProps.scrollbarWidth('value1', 'value2')).toEqual({
            scrollbarWidth: 'value1 value2'
        });
    });
    it('scrollBehavior type', () => {
        expect(typeof cssProps.scrollBehavior).toEqual('function');
    });

    it('scrollBehavior', () => {
        expect(cssProps.scrollBehavior('value1', 'value2')).toEqual({
            scrollBehavior: 'value1 value2'
        });
    });
    it('scrollMargin type', () => {
        expect(typeof cssProps.scrollMargin).toEqual('function');
    });

    it('scrollMargin', () => {
        expect(cssProps.scrollMargin('value1', 'value2')).toEqual({
            scrollMargin: 'value1 value2'
        });
    });
    it('scrollMarginBlock type', () => {
        expect(typeof cssProps.scrollMarginBlock).toEqual('function');
    });

    it('scrollMarginBlock', () => {
        expect(cssProps.scrollMarginBlock('value1', 'value2')).toEqual({
            scrollMarginBlock: 'value1 value2'
        });
    });
    it('scrollMarginBlockStart type', () => {
        expect(typeof cssProps.scrollMarginBlockStart).toEqual('function');
    });

    it('scrollMarginBlockStart', () => {
        expect(cssProps.scrollMarginBlockStart('value1', 'value2')).toEqual({
            scrollMarginBlockStart: 'value1 value2'
        });
    });
    it('scrollMarginBlockEnd type', () => {
        expect(typeof cssProps.scrollMarginBlockEnd).toEqual('function');
    });

    it('scrollMarginBlockEnd', () => {
        expect(cssProps.scrollMarginBlockEnd('value1', 'value2')).toEqual({
            scrollMarginBlockEnd: 'value1 value2'
        });
    });
    it('scrollMarginBottom type', () => {
        expect(typeof cssProps.scrollMarginBottom).toEqual('function');
    });

    it('scrollMarginBottom', () => {
        expect(cssProps.scrollMarginBottom('value1', 'value2')).toEqual({
            scrollMarginBottom: 'value1 value2'
        });
    });
    it('scrollMarginInline type', () => {
        expect(typeof cssProps.scrollMarginInline).toEqual('function');
    });

    it('scrollMarginInline', () => {
        expect(cssProps.scrollMarginInline('value1', 'value2')).toEqual({
            scrollMarginInline: 'value1 value2'
        });
    });
    it('scrollMarginInlineStart type', () => {
        expect(typeof cssProps.scrollMarginInlineStart).toEqual('function');
    });

    it('scrollMarginInlineStart', () => {
        expect(cssProps.scrollMarginInlineStart('value1', 'value2')).toEqual({
            scrollMarginInlineStart: 'value1 value2'
        });
    });
    it('scrollMarginInlineEnd type', () => {
        expect(typeof cssProps.scrollMarginInlineEnd).toEqual('function');
    });

    it('scrollMarginInlineEnd', () => {
        expect(cssProps.scrollMarginInlineEnd('value1', 'value2')).toEqual({
            scrollMarginInlineEnd: 'value1 value2'
        });
    });
    it('scrollMarginLeft type', () => {
        expect(typeof cssProps.scrollMarginLeft).toEqual('function');
    });

    it('scrollMarginLeft', () => {
        expect(cssProps.scrollMarginLeft('value1', 'value2')).toEqual({
            scrollMarginLeft: 'value1 value2'
        });
    });
    it('scrollMarginRight type', () => {
        expect(typeof cssProps.scrollMarginRight).toEqual('function');
    });

    it('scrollMarginRight', () => {
        expect(cssProps.scrollMarginRight('value1', 'value2')).toEqual({
            scrollMarginRight: 'value1 value2'
        });
    });
    it('scrollMarginTop type', () => {
        expect(typeof cssProps.scrollMarginTop).toEqual('function');
    });

    it('scrollMarginTop', () => {
        expect(cssProps.scrollMarginTop('value1', 'value2')).toEqual({
            scrollMarginTop: 'value1 value2'
        });
    });
    it('scrollPadding type', () => {
        expect(typeof cssProps.scrollPadding).toEqual('function');
    });

    it('scrollPadding', () => {
        expect(cssProps.scrollPadding('value1', 'value2')).toEqual({
            scrollPadding: 'value1 value2'
        });
    });
    it('scrollPaddingBlock type', () => {
        expect(typeof cssProps.scrollPaddingBlock).toEqual('function');
    });

    it('scrollPaddingBlock', () => {
        expect(cssProps.scrollPaddingBlock('value1', 'value2')).toEqual({
            scrollPaddingBlock: 'value1 value2'
        });
    });
    it('scrollPaddingBlockStart type', () => {
        expect(typeof cssProps.scrollPaddingBlockStart).toEqual('function');
    });

    it('scrollPaddingBlockStart', () => {
        expect(cssProps.scrollPaddingBlockStart('value1', 'value2')).toEqual({
            scrollPaddingBlockStart: 'value1 value2'
        });
    });
    it('scrollPaddingBlockEnd type', () => {
        expect(typeof cssProps.scrollPaddingBlockEnd).toEqual('function');
    });

    it('scrollPaddingBlockEnd', () => {
        expect(cssProps.scrollPaddingBlockEnd('value1', 'value2')).toEqual({
            scrollPaddingBlockEnd: 'value1 value2'
        });
    });
    it('scrollPaddingBottom type', () => {
        expect(typeof cssProps.scrollPaddingBottom).toEqual('function');
    });

    it('scrollPaddingBottom', () => {
        expect(cssProps.scrollPaddingBottom('value1', 'value2')).toEqual({
            scrollPaddingBottom: 'value1 value2'
        });
    });
    it('scrollPaddingInline type', () => {
        expect(typeof cssProps.scrollPaddingInline).toEqual('function');
    });

    it('scrollPaddingInline', () => {
        expect(cssProps.scrollPaddingInline('value1', 'value2')).toEqual({
            scrollPaddingInline: 'value1 value2'
        });
    });
    it('scrollPaddingInlineStart type', () => {
        expect(typeof cssProps.scrollPaddingInlineStart).toEqual('function');
    });

    it('scrollPaddingInlineStart', () => {
        expect(cssProps.scrollPaddingInlineStart('value1', 'value2')).toEqual({
            scrollPaddingInlineStart: 'value1 value2'
        });
    });
    it('scrollPaddingInlineEnd type', () => {
        expect(typeof cssProps.scrollPaddingInlineEnd).toEqual('function');
    });

    it('scrollPaddingInlineEnd', () => {
        expect(cssProps.scrollPaddingInlineEnd('value1', 'value2')).toEqual({
            scrollPaddingInlineEnd: 'value1 value2'
        });
    });
    it('scrollPaddingLeft type', () => {
        expect(typeof cssProps.scrollPaddingLeft).toEqual('function');
    });

    it('scrollPaddingLeft', () => {
        expect(cssProps.scrollPaddingLeft('value1', 'value2')).toEqual({
            scrollPaddingLeft: 'value1 value2'
        });
    });
    it('scrollPaddingRight type', () => {
        expect(typeof cssProps.scrollPaddingRight).toEqual('function');
    });

    it('scrollPaddingRight', () => {
        expect(cssProps.scrollPaddingRight('value1', 'value2')).toEqual({
            scrollPaddingRight: 'value1 value2'
        });
    });
    it('scrollPaddingTop type', () => {
        expect(typeof cssProps.scrollPaddingTop).toEqual('function');
    });

    it('scrollPaddingTop', () => {
        expect(cssProps.scrollPaddingTop('value1', 'value2')).toEqual({
            scrollPaddingTop: 'value1 value2'
        });
    });
    it('scrollSnapAlign type', () => {
        expect(typeof cssProps.scrollSnapAlign).toEqual('function');
    });

    it('scrollSnapAlign', () => {
        expect(cssProps.scrollSnapAlign('value1', 'value2')).toEqual({
            scrollSnapAlign: 'value1 value2'
        });
    });
    it('scrollSnapStop type', () => {
        expect(typeof cssProps.scrollSnapStop).toEqual('function');
    });

    it('scrollSnapStop', () => {
        expect(cssProps.scrollSnapStop('value1', 'value2')).toEqual({
            scrollSnapStop: 'value1 value2'
        });
    });
    it('scrollSnapType type', () => {
        expect(typeof cssProps.scrollSnapType).toEqual('function');
    });

    it('scrollSnapType', () => {
        expect(cssProps.scrollSnapType('value1', 'value2')).toEqual({
            scrollSnapType: 'value1 value2'
        });
    });
    it('shapeImageThreshold type', () => {
        expect(typeof cssProps.shapeImageThreshold).toEqual('function');
    });

    it('shapeImageThreshold', () => {
        expect(cssProps.shapeImageThreshold('value1', 'value2')).toEqual({
            shapeImageThreshold: 'value1 value2'
        });
    });
    it('shapeMargin type', () => {
        expect(typeof cssProps.shapeMargin).toEqual('function');
    });

    it('shapeMargin', () => {
        expect(cssProps.shapeMargin('value1', 'value2')).toEqual({ shapeMargin: 'value1 value2' });
    });
    it('shapeOutside type', () => {
        expect(typeof cssProps.shapeOutside).toEqual('function');
    });

    it('shapeOutside', () => {
        expect(cssProps.shapeOutside('value1', 'value2')).toEqual({
            shapeOutside: 'value1 value2'
        });
    });
    it('tabSize type', () => {
        expect(typeof cssProps.tabSize).toEqual('function');
    });

    it('tabSize', () => {
        expect(cssProps.tabSize('value1', 'value2')).toEqual({ tabSize: 'value1 value2' });
    });
    it('tableLayout type', () => {
        expect(typeof cssProps.tableLayout).toEqual('function');
    });

    it('tableLayout', () => {
        expect(cssProps.tableLayout('value1', 'value2')).toEqual({ tableLayout: 'value1 value2' });
    });
    it('textAlign type', () => {
        expect(typeof cssProps.textAlign).toEqual('function');
    });

    it('textAlign', () => {
        expect(cssProps.textAlign('value1', 'value2')).toEqual({ textAlign: 'value1 value2' });
    });
    it('textAlignLast type', () => {
        expect(typeof cssProps.textAlignLast).toEqual('function');
    });

    it('textAlignLast', () => {
        expect(cssProps.textAlignLast('value1', 'value2')).toEqual({
            textAlignLast: 'value1 value2'
        });
    });
    it('textCombineUpright type', () => {
        expect(typeof cssProps.textCombineUpright).toEqual('function');
    });

    it('textCombineUpright', () => {
        expect(cssProps.textCombineUpright('value1', 'value2')).toEqual({
            textCombineUpright: 'value1 value2'
        });
    });
    it('textDecoration type', () => {
        expect(typeof cssProps.textDecoration).toEqual('function');
    });

    it('textDecoration', () => {
        expect(cssProps.textDecoration('value1', 'value2')).toEqual({
            textDecoration: 'value1 value2'
        });
    });
    it('textDecorationColor type', () => {
        expect(typeof cssProps.textDecorationColor).toEqual('function');
    });

    it('textDecorationColor', () => {
        expect(cssProps.textDecorationColor('value1', 'value2')).toEqual({
            textDecorationColor: 'value1 value2'
        });
    });
    it('textDecorationLine type', () => {
        expect(typeof cssProps.textDecorationLine).toEqual('function');
    });

    it('textDecorationLine', () => {
        expect(cssProps.textDecorationLine('value1', 'value2')).toEqual({
            textDecorationLine: 'value1 value2'
        });
    });
    it('textDecorationSkipInk type', () => {
        expect(typeof cssProps.textDecorationSkipInk).toEqual('function');
    });

    it('textDecorationSkipInk', () => {
        expect(cssProps.textDecorationSkipInk('value1', 'value2')).toEqual({
            textDecorationSkipInk: 'value1 value2'
        });
    });
    it('textDecorationStyle type', () => {
        expect(typeof cssProps.textDecorationStyle).toEqual('function');
    });

    it('textDecorationStyle', () => {
        expect(cssProps.textDecorationStyle('value1', 'value2')).toEqual({
            textDecorationStyle: 'value1 value2'
        });
    });
    it('textDecorationThickness type', () => {
        expect(typeof cssProps.textDecorationThickness).toEqual('function');
    });

    it('textDecorationThickness', () => {
        expect(cssProps.textDecorationThickness('value1', 'value2')).toEqual({
            textDecorationThickness: 'value1 value2'
        });
    });
    it('textEmphasis type', () => {
        expect(typeof cssProps.textEmphasis).toEqual('function');
    });

    it('textEmphasis', () => {
        expect(cssProps.textEmphasis('value1', 'value2')).toEqual({
            textEmphasis: 'value1 value2'
        });
    });
    it('textEmphasisColor type', () => {
        expect(typeof cssProps.textEmphasisColor).toEqual('function');
    });

    it('textEmphasisColor', () => {
        expect(cssProps.textEmphasisColor('value1', 'value2')).toEqual({
            textEmphasisColor: 'value1 value2'
        });
    });
    it('textEmphasisPosition type', () => {
        expect(typeof cssProps.textEmphasisPosition).toEqual('function');
    });

    it('textEmphasisPosition', () => {
        expect(cssProps.textEmphasisPosition('value1', 'value2')).toEqual({
            textEmphasisPosition: 'value1 value2'
        });
    });
    it('textEmphasisStyle type', () => {
        expect(typeof cssProps.textEmphasisStyle).toEqual('function');
    });

    it('textEmphasisStyle', () => {
        expect(cssProps.textEmphasisStyle('value1', 'value2')).toEqual({
            textEmphasisStyle: 'value1 value2'
        });
    });
    it('textIndent type', () => {
        expect(typeof cssProps.textIndent).toEqual('function');
    });

    it('textIndent', () => {
        expect(cssProps.textIndent('value1', 'value2')).toEqual({ textIndent: 'value1 value2' });
    });
    it('textJustify type', () => {
        expect(typeof cssProps.textJustify).toEqual('function');
    });

    it('textJustify', () => {
        expect(cssProps.textJustify('value1', 'value2')).toEqual({ textJustify: 'value1 value2' });
    });
    it('textOrientation type', () => {
        expect(typeof cssProps.textOrientation).toEqual('function');
    });

    it('textOrientation', () => {
        expect(cssProps.textOrientation('value1', 'value2')).toEqual({
            textOrientation: 'value1 value2'
        });
    });
    it('textOverflow type', () => {
        expect(typeof cssProps.textOverflow).toEqual('function');
    });

    it('textOverflow', () => {
        expect(cssProps.textOverflow('value1', 'value2')).toEqual({
            textOverflow: 'value1 value2'
        });
    });
    it('textRendering type', () => {
        expect(typeof cssProps.textRendering).toEqual('function');
    });

    it('textRendering', () => {
        expect(cssProps.textRendering('value1', 'value2')).toEqual({
            textRendering: 'value1 value2'
        });
    });
    it('textShadow type', () => {
        expect(typeof cssProps.textShadow).toEqual('function');
    });

    it('textShadow', () => {
        expect(cssProps.textShadow('value1', 'value2')).toEqual({ textShadow: 'value1 value2' });
    });
    it('textTransform type', () => {
        expect(typeof cssProps.textTransform).toEqual('function');
    });

    it('textTransform', () => {
        expect(cssProps.textTransform('value1', 'value2')).toEqual({
            textTransform: 'value1 value2'
        });
    });
    it('textUnderlineOffset type', () => {
        expect(typeof cssProps.textUnderlineOffset).toEqual('function');
    });

    it('textUnderlineOffset', () => {
        expect(cssProps.textUnderlineOffset('value1', 'value2')).toEqual({
            textUnderlineOffset: 'value1 value2'
        });
    });
    it('textUnderlinePosition type', () => {
        expect(typeof cssProps.textUnderlinePosition).toEqual('function');
    });

    it('textUnderlinePosition', () => {
        expect(cssProps.textUnderlinePosition('value1', 'value2')).toEqual({
            textUnderlinePosition: 'value1 value2'
        });
    });
    it('top type', () => {
        expect(typeof cssProps.top).toEqual('function');
    });

    it('top', () => {
        expect(cssProps.top('value1', 'value2')).toEqual({ top: 'value1 value2' });
    });
    it('touchAction type', () => {
        expect(typeof cssProps.touchAction).toEqual('function');
    });

    it('touchAction', () => {
        expect(cssProps.touchAction('value1', 'value2')).toEqual({ touchAction: 'value1 value2' });
    });
    it('transform type', () => {
        expect(typeof cssProps.transform).toEqual('function');
    });

    it('transform', () => {
        expect(cssProps.transform('value1', 'value2')).toEqual({ transform: 'value1 value2' });
    });
    it('transformBox type', () => {
        expect(typeof cssProps.transformBox).toEqual('function');
    });

    it('transformBox', () => {
        expect(cssProps.transformBox('value1', 'value2')).toEqual({
            transformBox: 'value1 value2'
        });
    });
    it('transformOrigin type', () => {
        expect(typeof cssProps.transformOrigin).toEqual('function');
    });

    it('transformOrigin', () => {
        expect(cssProps.transformOrigin('value1', 'value2')).toEqual({
            transformOrigin: 'value1 value2'
        });
    });
    it('transformStyle type', () => {
        expect(typeof cssProps.transformStyle).toEqual('function');
    });

    it('transformStyle', () => {
        expect(cssProps.transformStyle('value1', 'value2')).toEqual({
            transformStyle: 'value1 value2'
        });
    });
    it('transition type', () => {
        expect(typeof cssProps.transition).toEqual('function');
    });

    it('transition', () => {
        expect(cssProps.transition('value1', 'value2')).toEqual({ transition: 'value1 value2' });
    });
    it('transitionDelay type', () => {
        expect(typeof cssProps.transitionDelay).toEqual('function');
    });

    it('transitionDelay', () => {
        expect(cssProps.transitionDelay('value1', 'value2')).toEqual({
            transitionDelay: 'value1 value2'
        });
    });
    it('transitionDuration type', () => {
        expect(typeof cssProps.transitionDuration).toEqual('function');
    });

    it('transitionDuration', () => {
        expect(cssProps.transitionDuration('value1', 'value2')).toEqual({
            transitionDuration: 'value1 value2'
        });
    });
    it('transitionProperty type', () => {
        expect(typeof cssProps.transitionProperty).toEqual('function');
    });

    it('transitionProperty', () => {
        expect(cssProps.transitionProperty('value1', 'value2')).toEqual({
            transitionProperty: 'value1 value2'
        });
    });
    it('transitionTimingFunction type', () => {
        expect(typeof cssProps.transitionTimingFunction).toEqual('function');
    });

    it('transitionTimingFunction', () => {
        expect(cssProps.transitionTimingFunction('value1', 'value2')).toEqual({
            transitionTimingFunction: 'value1 value2'
        });
    });
    it('translate type', () => {
        expect(typeof cssProps.translate).toEqual('function');
    });

    it('translate', () => {
        expect(cssProps.translate('value1', 'value2')).toEqual({ translate: 'value1 value2' });
    });
    it('unicodeBidi type', () => {
        expect(typeof cssProps.unicodeBidi).toEqual('function');
    });

    it('unicodeBidi', () => {
        expect(cssProps.unicodeBidi('value1', 'value2')).toEqual({ unicodeBidi: 'value1 value2' });
    });
    it('userSelect type', () => {
        expect(typeof cssProps.userSelect).toEqual('function');
    });

    it('userSelect', () => {
        expect(cssProps.userSelect('value1', 'value2')).toEqual({ userSelect: 'value1 value2' });
    });
    it('verticalAlign type', () => {
        expect(typeof cssProps.verticalAlign).toEqual('function');
    });

    it('verticalAlign', () => {
        expect(cssProps.verticalAlign('value1', 'value2')).toEqual({
            verticalAlign: 'value1 value2'
        });
    });
    it('visibility type', () => {
        expect(typeof cssProps.visibility).toEqual('function');
    });

    it('visibility', () => {
        expect(cssProps.visibility('value1', 'value2')).toEqual({ visibility: 'value1 value2' });
    });
    it('whiteSpace type', () => {
        expect(typeof cssProps.whiteSpace).toEqual('function');
    });

    it('whiteSpace', () => {
        expect(cssProps.whiteSpace('value1', 'value2')).toEqual({ whiteSpace: 'value1 value2' });
    });
    it('widows type', () => {
        expect(typeof cssProps.widows).toEqual('function');
    });

    it('widows', () => {
        expect(cssProps.widows('value1', 'value2')).toEqual({ widows: 'value1 value2' });
    });
    it('width type', () => {
        expect(typeof cssProps.width).toEqual('function');
    });

    it('width', () => {
        expect(cssProps.width('value1', 'value2')).toEqual({ width: 'value1 value2' });
    });
    it('willChange type', () => {
        expect(typeof cssProps.willChange).toEqual('function');
    });

    it('willChange', () => {
        expect(cssProps.willChange('value1', 'value2')).toEqual({ willChange: 'value1 value2' });
    });
    it('wordBreak type', () => {
        expect(typeof cssProps.wordBreak).toEqual('function');
    });

    it('wordBreak', () => {
        expect(cssProps.wordBreak('value1', 'value2')).toEqual({ wordBreak: 'value1 value2' });
    });
    it('wordSpacing type', () => {
        expect(typeof cssProps.wordSpacing).toEqual('function');
    });

    it('wordSpacing', () => {
        expect(cssProps.wordSpacing('value1', 'value2')).toEqual({ wordSpacing: 'value1 value2' });
    });
    it('wordWrap type', () => {
        expect(typeof cssProps.wordWrap).toEqual('function');
    });

    it('wordWrap', () => {
        expect(cssProps.wordWrap('value1', 'value2')).toEqual({ wordWrap: 'value1 value2' });
    });
    it('writingMode type', () => {
        expect(typeof cssProps.writingMode).toEqual('function');
    });

    it('writingMode', () => {
        expect(cssProps.writingMode('value1', 'value2')).toEqual({ writingMode: 'value1 value2' });
    });
    it('zIndex type', () => {
        expect(typeof cssProps.zIndex).toEqual('function');
    });

    it('zIndex', () => {
        expect(cssProps.zIndex('value1', 'value2')).toEqual({ zIndex: 'value1 value2' });
    });
});
