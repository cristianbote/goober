//Do not modify this file.  This file is generated from ./scripts/generateProps.js
import { default as appendArgs } from '../../util/index'

/**
 * @name backfaceVisibility
 * @category CssProps Helper
 * @summary Returns css property backfaceVisibility and value as an object
 *
 * @description
 * Returns an object with the css property backfaceVisibility with its passed in values
 *
 * @param - One or more parameters that will be appended together based on a delimiter
 * @returns backfaceVisibility property as object
 */

export default function backfaceVisibility() {
    return {
        backfaceVisibility: appendArgs(arguments)
    }
}