/* Do not modify this file.  This file is generated from ./cssProps/templates/constants.test.mustache */
import * as constants from '../src/constants/index';
import { color } from '../src/props/index';
import { css, extractCss } from 'goober';

describe('cssAtRules', () => {
{{#Constants}}
    it('{{constantName}} type', () => {
        expect(typeof constants.{{constantName}}).toEqual("string");
    });
    it('{{constantName}}', () => {
        css([
            color(constants.{{constantName}}),
        ]);
        expect(extractCss().replace(/go\d+/, "go12345")).toEqual(".go12345{color:{{constantValue}};}")
    });

{{/Constants}}
});
