/* Do not modify this file.  This file is generated from ./cssProps/templates/pseudos.test.mustache */
import * as cssPseudo from '../src/pseudos/index';
import { color, maxWidth } from '../src/props/index'
import { css } from 'goober';

describe('cssPseudo', () => {
{{#Pseudos}}
    it('{{pseudo}} type', () => {
        expect(typeof cssPseudo.{{pseudoCamelCase}}).toEqual('function');
    });

    it('{{pseudo}}', () => {
        expect(cssPseudo.{{pseudoCamelCase}}([
            color("blue"),
            maxWidth("20px")
        ])).toEqual({"&{{pseudo}}":{
            color: "blue",
            maxWidth: "20px"
        }});
    });

{{/Pseudos}}
});
